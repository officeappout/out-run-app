# שלב 3 — מטריצת סיכונים (מסגור: השקה אמיתית, לא דמו)

**מסגור מחייב (דוד, 19.09.2026):** שדרות היא השקה אמיתית — תושבים אמיתיים, שילוט פיזי קבוע, ואין מי שילווה תושב שנכנס לבד. חומרה נמדדת לפי **השפעה על תושב לא מלווה**, ונזק לדאטה (היסטוריה/XP/דירוגים) נחשב **בלתי הפיך**, לא "ננקה אחר כך".

**חשוב לפני המטריצה — תיקון לממצא של שלב 2:** הניסוח "Infinity → תמיד מחוץ למסלול" ב-`02-verification-results.md §1` היה **לא מדויק**. אימות נוסף (למטה, סעיף 3) מראה שההתנהגות האמיתית שונה — ולדעתי חמורה יותר בדרך אחרת. מתקן כאן.

---

## תוספת 1 — כמה נקודות במסלול ההליכה היחיד בשדרות

**קריאה בלבד, מאומת:** `official_routes/YVx4pgJjOOXR8j497pPF` (היחיד המתויג `walking` בשדרות) — **164 נקודות, זוגי**.

**המשמעות הישירה:** `isOutAndBackPath` (`geoUtils.ts:282`) בודק `path.length % 2 === 0 → return false` **לפני** כל destructuring — אז דווקא המסלול הזה **לא** יקרוס דרך המנגנון שנמצא בשלב 2 (§1 למטה) אם ייפתח בסשן מודרך/קבוצתי. **אבל** — אימתתי גם שהנתונים הגולמיים שלו מאוחסנים בדיוק כמו שהתיעוד טוען (`{lng,lat}` אובייקטים, לא tuples): `path[0] = {"lng":34.603907,"lat":31.531278}`. כשהוא נשלף דרך הנתיב הפגום (`FreeRunLayer.tsx`), `buildLaneOffsetPath` יחזיר אותו **ללא שינוי** (עדיין אובייקטים) — והוא ייכנס כ-`coordinates` של GeoJSON LineString ל-Mapbox. **מה Mapbox עושה עם זה בפועל — `[לא אומת]`, דורש דפדפן/מכשיר אמיתי, לא ניתן להוכיח מ-Node.**

---

## תוספת 2 — מאיזה אוסף המסך האמיתי טוען מסלולי הליכה

**המסקנה מתקנת את ההשערה ב-2 שכבות — לא "ריק לגמרי", אבל גם לא "בטוח לגמרי":**

1. **המסך המרכזי (גילוי/בחירת מסלול על המפה):** `useRouteGeneration.ts:87` → `official_routes`. **זה מאוכלס** (27 מסמכים בשדרות). תושב שפותח את המפה ומחפש מסלול הליכה **לא** יראה רשימה ריקה.
2. **אבל שתי מסכים משניים אמיתיים קוראים אך ורק מ-`curated_routes`, שריק לחלוטין בשדרות (0 מסמכים):**
   - **`fetchHeroRoute`** (`location-utils.ts:1298-1354`, קרוא מ-`UnifiedLocationStep.tsx:368,458,681`) — מציג כרטיס "מסלול נבחר" בתהליך ה-**onboarding** (חוויה ראשונה של תושב חדש). כשהאוסף ריק, הפונקציה מחזירה `null` בשקט (`location-utils.ts:1352`) וה-UI מסתיר את הכרטיס לגמרי (`UnifiedLocationStep.tsx:852`) — **לא קורס, לא מציג "0 מסלולים", פשוט חסר.** תושב שדרות חדש לא יקבל את כרטיס-ה"הצצה" הזה בהרשמה, בניגוד לעיר עם curated_routes מאוכלס.
   - **`PlannedActivityComposeSheet.tsx:205`** ("תכנון פעילות", בחירת מיקום מסוג "מסלול") — קורא `fetchCuratedRoutesByAuthority` **בלי fallback ל-official_routes**. תושב שדרות שפותח את הבורר הזה **כן יראה רשימה ריקה בפועל** (`routeOptions` ריק, `PlannedActivityComposeSheet.tsx:225-230`).

**סיווג שלי (לא P0):** זה אמיתי, אבל לא "המסך הראשי שתושב לבד פותח כדי לצאת להליכה" — זה תכונה משנית (תכנון-פעילות + כרטיס-הצצה בהרשמה). מסמן **P1**, לא P0. אם דוד רואה בכרטיס ה-onboarding משקל גדול יותר להשקה (רושם ראשוני) — זה שיקול-דעת מוצרי, לא טכני, ואפשר להעלות בהתאם.

---

## תוספת 3 — "תמיד מחוץ למסלול": תיקון + הוכחה ברמת UI

**הוכחה מלאה + קוד:** `scripts/_verify-deviation-inert-on-corrupt-geometry.ts` (הורץ).

**התיקון לממצא שלב 2:** קראתי את השומר האמיתי ב-`useRunningPlayer.ts:481`: `if (!Number.isFinite(distMeters)) return;`. **`Infinity` נכשל ב-`Number.isFinite` ולכן השומר יוצא החוצה *לפני* הלוגיקה של "מחוץ למסלול".** המשמעות האמיתית — הפוכה ממה שכתבתי בשלב 2:

| מצב | `crossTrackDistanceMeters` בפועל | מה קורה בפועל |
|---|---|---|
| גיאומטריה תקינה, משתמש **על** המסלול | 14.4 מ' | תקין, שקט |
| גיאומטריה תקינה, משתמש **2 ק"מ** מחוץ למסלול | 1100 מ' | **מפעיל** את ההתראה הקולית + ניתוב-מחדש (מוכח: `isOffRoute→true`, `offRouteEventToken` עולה) |
| גיאומטריה **פגומה** (הבאג), משתמש על המסלול | `Infinity` | **כלום** — לא נדלק שום דבר |
| גיאומטריה **פגומה** (הבאג), משתמש 2 ק"מ מחוץ למסלול | `Infinity` | **גם כלום** — אותה תוצאה בדיוק |

**ברמת UI (`useRouteDeviationOrchestrator.ts:1-37`):** ה"התראה" האמיתית היא הכרזה קולית בעברית — `audioService.speak('סטית מהמסלול, מחשב מסלול מחדש לסיום האימון')` — ואז **המסלול על המפה מוחלף לגמרי** במסלול מזוהה-דינמית חדש. זו ההתנהגות שאמורה לקרות כשמשתמש באמת סוטה. **המסקנה המתוקנת:** בסשן מודרך/קבוצתי עם גיאומטריה פגומה, ההכרזה הזו **לעולם לא תישמע ולא יקרה ניתוב-מחדש — גם אם המשתמש באמת הולך לאיבוד.** זו לא "אזעקת שווא" — זו **רשת ביטחון כבויה לגמרי**, בשקט, בלי שום אינדיקציה למשתמש שהיא כבויה.

---

## תוספת 4 — סיווג לפי המסגור החדש

### קריסת AppMap — תקלה חיה היום, לא סיכון עתידי

(אשקלון הוסרה מהניתוח — אין שם מסלולים בכוונה, לא רלוונטי לבדיקה הזאת.)

ספרתי גיאומטריה **בכל הרשויות שיש בהן מסלולים**, לא רק בשדרות: **94 מתוך 192 מסמכי `official_routes` (49%!) ו-6 מתוך 18 `curated_routes` הם באורך אי-זוגי — כלומר קורסים דרך המנגנון הזה.** לפי עיר:

| עיר | מסלולים אי-זוגיים (crash-prone) |
|---|---|
| שדרות | **14** |
| חיפה | 39 |
| הרצליה | 18 |
| זכרון יעקב | 12 |
| תל אביב-יפו | 11 (+6 curated_routes) |

**זה אומר: 14 מהמסלולים של שדרות עצמה חשופים לקריסה הזאת היום, בפרודקשן החי, בלי לגעת בקוד.** מסלול ההליכה היחיד שנבדק (164, זוגי) בטוח מהמנגנון הספציפי הזה — אבל אם סשן קבוצתי/מודרך בשדרות (או כל עיר אחרת) ייגע באחד מ-13 המסלולים האחרים (רובם מתויגים `running`, אבל משתמש יכול לבחור "הליכה" כפעילות על כל מסלול, ללא קשר לתיוג האחסון שלו) — מסך המפה קורס.

**גודל ה-diff:** קטן וממוקד. שינוי אחד ב-`FreeRunLayer.tsx` (השורה שקוראת `data.path`) — לעטוף ב-`normalizeStoredRoutePath` (כבר קיים, כבר בשימוש בכל שאר הריפו). ~3-5 שורות, קובץ אחד, ללא שינוי סכמה, ללא מיגרציית דאטה.

**הדרך הבטוחה ביותר להוציא, בסביבה שבה merge=פרודקשן מיידי:** זה בדיוק סוג התיקון שראוי להוציא **מהר ובנפרד** — לא כחלק ממקבץ:
1. תיקון קו-בודד + הוספת הבדיקה שכבר כתבתי (`scripts/_verify-freerunlayer-geometry-nan.ts`) כטסט רגרסיה קבוע בסוויטה (`npm test`), לא רק סקריפט חד-פעמי.
2. סיכון-לוואי כמעט אפסי: הפונקציה שמוחלפת פנימה (`normalizeStoredRoutePath`) היא read-only, טהורה, וכבר משמשת בכל נתיב קריאה *אחר* של אותה סכימה בדיוק — אין תרחיש שבו זה **מחמיר** משהו שעובד היום.
3. עדיין מומלץ: סמוק-טסט ידני על מכשיר אמיתי (סשן קבוצתי על מסלול אי-זוגי) לפני push, כי ההשפעה על Mapbox בפועל לא נבדקה (תוספת 1).
4. זה מועמד ל"hotfix" אמיתי — לא לחכות לשאר הממצאים כדי לתקן, בגלל ה-49% שכבר חי.

### הליכה שנרשמת כ-running — נזק דאטה מצטבר, לא קוסמטי. תאריך התחלה מזוהה.

**חפרתי בהיסטוריית git, לא ניחשתי:** הקו הפגום עצמו (`workoutType: 'running'` קבוע) קיים **מאז ה-2.3.2026** (`1ad483088`) — מאז ש-`syncWorkoutCompletion` נכתב לראשונה, כשעדיין לא הייתה בכלל אבחנה אמיתית בין הליכה לריצה בקוד. **הבאג עצמו לא "נשבר" — הוא פשוט אף פעם לא עודכן** כש-`activityType:'walking'` הפך לבחירה אמיתית של משתמש, ב-**7.7.2026** (`1db08d29`, "Hybrid Phase 0 — revive dormant session-store plumbing") — זה הקומיט שבו `FreeRunDrawer`'s צ'יפ ה"הליכה" **התחיל בפועל** לקרוא ל-`setActivityType('walking')`. לפני התאריך הזה, אין משמעות לבאג — לא היה מה לתייג לא-נכון, כי הליכה לא הייתה מצב אמיתי-נבדל בקוד.

**מסקנה: הנזק מצטבר מ-7.7.2026 ועד היום (19.9.2026) — כ-74 יום** — על **כל** משתמש בכל עיר שסיים סשן הליכה חופשי דרך `FreeRunDrawer`, לא רק שדרות. כל `dailyProgress` שנכתב עבור הליכה בחלון הזה נושא `workoutType:'running'`/`displayIcon:'run-fast'` לצמיתות (אלא אם ייכתב backfill נפרד), וכל מרחק הליכה כזה כבר נספר בפועל בתוך `useWeeklyVolumeStore`'s מעקב **ריצה** (`completion-sync.service.ts:104-107`) — לא ניתן "לנקות" את זה בדיעבד בלי סקריפט backfill ייעודי, שהוא **שאלת דאטה נפרדת** ולא חלק מהתיקון הזה.

**חשוב לטסט בשדרות ספציפית:** מרגע ההשקה, **כל תושב חדש שילך** יוסיף לאותה השחתה, מרגע ראשון — לא "אם לא נתקן זה יישבר יום אחד", אלא "ההשחתה ממשיכה מדקה 1 של ההשקה, בדיוק כמו בכל עיר אחרת מ-7.7."

**גודל ה-diff:** קטן מאוד — 2 שורות ב-`useRunningPlayer.ts` (1848, 1852: `'running'`→ ביטוי מותנה על `activityType`), אופציונלי גם `FreeRunSummary.tsx:179` (טקסט שיתוף, קוסמטי, לא כותב לדאטה).

**הדרך הבטוחה להוציא:** תיקון-ערך-קדימה בלבד, לא נוגע בשום נתיב כתיבה/טרנזקציה/כלל Firestore. בטוח לצרף לאותו hotfix כמו קריסת ה-AppMap, או להוציא בנפרד מיד אחריו — שניהם באותה רמת סיכון-לוואי נמוכה. **מה שהתיקון הזה *לא* פותר:** את 74 הימים שכבר נכתבו. זו החלטה נפרדת (backfill דורש קודם למפות איזה `workouts/{id}` docs הם באמת `activityType:'walking'` שהושלמו בחלון הזה, ולתקן את ה-`dailyProgress`/`weeklyVolume` המתאימים — לא בנוי, לא מתוכנן כאן).

---

## מטריצה מלאה

| # | ממצא | חומרה | מה יישבר בשטח | איך אומת | תיקון מוצע |
|---|---|---|---|---|---|
| 1 | קריסת `AppMap` בסשן מודרך/קבוצתי על מסלול אי-זוגי (`FreeRunLayer.tsx:102-133` עוקף נרמול) | **P0 — חי בפרודקשן היום, 14/27 מהמסלולים של שדרות עצמה חשופים** | תושב/קבוצה בסשן מודרך על אחד מ-13 המסלולים הלא-הליכה של שדרות → מסך מפה **נעלם ומוחלף** במסך "המפה נתקלה בבעיה / נסה שוב" (`src/app/map/error.tsx` — Next.js תופס את זה, לא white-screen גולמי) | הרצה: `TypeError` אמיתי נתפס, מסלול תגובה מלא מאומת (`AppMap.tsx:1234,1262`) | ~3-5 שורות, קובץ אחד, `normalizeStoredRoutePath` קיים; ראו למעלה לפירוט שיפור |

**תיקון חשוב (נמצא אחרי כתיבת השורה למעלה):** יש `error.tsx` אמיתי ב-`src/app/map/` שתופס את הקריסה ומציג מסך עברי ידידותי — "המפה נתקלה בבעיה... הנתונים שלך נשמרו" עם כפתור "נסה שוב". זה **לא** white-screen גולמי, וזו רשת ביטחון אמיתית שכבר קיימת. **אבל**: `reset()` שהכפתור מפעיל רק מרנדר מחדש את אותו component tree — אם ה-state הפגום (`groupRoute`) לא מתנקה, "נסה שוב" עלול לקרוס מיד שוב באותו לולאה. **`[לא אומת]` — זה בדיוק הדבר הראשון לבדוק בבדיקת המכשיר של 48 שעות (שלב 4, סעיף 0).**
| 2 | רשת-ביטחון סטייה-מהמסלול כבויה לגמרי בגיאומטריה פגומה (לא "אזעקת שווא" כפי שנכתב בשלב 2) | **P1 — משפיע רק על סשנים מודרכים/קבוצתיים עם גיאומטריה פגומה; אין בטיחות אם משתמש באמת הולך לאיבוד** | משתמש בסשן קבוצתי סוטה באמת מהמסלול → לא נשמעת הכרזה, לא מנותב מחדש | הרצה: השוואת control/bug על אותה פונקציה אמיתית + קריאת שומר מקורי | נפתר אוטומטית עם תיקון #1 (אותו root cause) |
| 3 | הליכה נרשמת כ-`'running'` ב-`dailyProgress` + מזהמת את מעקב הנפח השבועי של ריצה | **P1 — נזק דאטה מצטבר ובלתי הפיך, לא קוסמטי; מצטבר מ-7.7.2026 (~74 יום), ימשיך מרגע ההשקה** | לא נראה גלוי למשתמש באירוע; משחית את הרשומה ההיסטורית/דירוגים שלו לצמיתות | הרצה: קוד production אמיתי, 4/4 טסטים, פלט JSON מלא | 2 שורות, `useRunningPlayer.ts:1848,1852`; **backfill להיסטוריה הקיימת = החלטה נפרדת, לא כלול** |
| 4 | `curated_routes` ריק בשדרות → כרטיס "הצצה" ב-onboarding חסר + בורר-מסלול ב"תכנון פעילות" ריק | **P1 — לא P0: המסך הראשי לגילוי מסלול (official_routes) מאוכלס ותקין** | תושב חדש לא רואה כרטיס הצצה בהרשמה; משתמש שפותח "תכנון פעילות→מסלול" רואה רשימה ריקה | קריאה + מעקב קוד מדויק (`fetchHeroRoute`, `PlannedActivityComposeSheet.tsx`) — לא הורץ בדפדפן | הוספת fallback ל-`official_routes` כש-`curated_routes` ריק, קובץ אחד; או תוכן (seed) — החלטה מוצרית |
| 5 | קואורדינטות `{lng,lat}` (לא tuples) מגיעות ל-Mapbox `LineString.coordinates` ללא נרמול, גם במסלולים זוגיים-שלא-קורסים (כולל מסלול ההליכה היחיד של שדרות, 164 נק') | **`[לא אומת]` — לא ניתן להוכיח מ-Node, דורש דפדפן/מכשיר אמיתי** | לא ידוע: קו מסלול לא מוצג / שגיאת Mapbox פנימית / משהו אחר | קריאה + מבנה-דאטה אמיתי מאומת; רינדור בפועל לא נבדק | נפתר אוטומטית עם תיקון #1 |

---

## עצירה

אפס תיקונים, אפס merge, אפס push. שני תיקוני P0/P1 שזוהו (#1+#2 יחד, #3) קטנים מאוד ונפרדים זה מזה — ממתין לאישורך המפורש לפני נגיעה בקוד, כולל סדר העדיפויות (להתחיל מ-#1 כפי שביקשת).
