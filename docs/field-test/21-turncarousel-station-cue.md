# B — TurnCarousel קלף תחנה (kind='station') — נכתב ונבדק — 22.09.2026

## ענף נפרד, מבוסס על origin/main עדכני

`feat/turncarousel-station-cue`, נוצר `git worktree add ... origin/main` טרי (`92b4fe39`, לא `d78feab6` שממנו נוצר ענף התחנות — origin/main התקדם עוד בינתיים). לא merge, לא push.

## מה השתנה — 4 קבצים

1. **`src/features/parks/core/services/geoUtils.ts`** — שדה `kind?: 'station'` + `parkId?: string` חדשים על `RouteTurn` (אופציונליים — לא שוברים אף צרכן קיים כמו `RouteDetailSheet`/ה-HUD הישן).
2. **`src/features/parks/core/components/turn-carousel-logic.ts`** — **קובץ חדש**, `.ts` רגיל (לא `.tsx`). מכיל את כל הלוגיקה הטהורה שהועברה מ-`TurnCarousel.tsx`: `getIconForInstruction`, `findNearestPathIdx`, ושתי פונקציות חדשות — `buildAllTurns` (ממזגת תחנות לתוך רשימת הפניות, לפי סדר-מסלול אמיתי דרך snap ל-vertex הקרוב) ו-`computeCurrentGpsIdx` (מוצא את הקלף הבא-בתור, כולל תחנות).

   **למה קובץ נפרד:** בדקתי בפועל — ייבוא ישיר של `TurnCarousel.tsx` (אפילו רק ל-named exports טהורים, בלי רינדור) נכשל תחת vitest של הריפו הזה (`AggregateError: oxc transform error` ב-`vite:define`, על קובץ `.tsx` — לא קשור בכלל לחוסר jsdom). זו הדרך היחידה שמצאתי לבדוק את הלוגיקה הזו באמת, לא ב-mirror/העתקה.

3. **`src/features/parks/core/components/TurnCarousel.tsx`** — מייבא את 4 הפונקציות מהקובץ החדש במקום להגדיר אותן מקומית (אותה לוגיקה בדיוק, לא שוכפלה). שינויים בפועל ברכיב עצמו:
   - `stations` prop חדש.
   - `getIconForInstruction`/`findNearestPathIdx` — נקראים עם החתימה החדשה (מקבלים את כל אובייקט ה-turn, לא רק instruction).
   - `isStation` נוסף לצד `isDestination` בשני מקומות (THIN + FULL render), עם טקסט "התחנה הבאה" (`STATION_NEXT_LABEL`) במקום "תפנייה הבאה" כשהקלף המודגש הוא תחנה.
   - `useStreetNamesForTurns` מדלג על תחנות (לא מבזבז קריאת Mapbox geocode על שם שכבר יש לו).
4. **`src/app/map/MapShell.tsx`** — שורה אחת: `stations={(logic.focusedRoute as any)?.stationMarkers ?? null}`, אותו מקור בדיוק כמו `hybridStations` שכבר מוזן ל-AppMap.

## מדוע בטוח לניווט רגיל (5 המסכים החיים)

**עם `stations` ריק/undefined — הפלט זהה ביט-לביט להתנהגות הישנה** (מוכח בטסט, לא רק בטענה): `buildAllTurns(turns, path, undefined)` שקול בדיוק ל"turns + כרטיס יעד", ו-`computeCurrentGpsIdx` נסרק כעת מול `allTurns` המלא (במקום `turns` בלבד) — שינוי טכני נדרש כדי שתחנה שמוכנסת *באמצע* הרשימה (לא רק בסוף) תימצא נכון; **הוכח שקול** להתנהגות הישנה על פני סוויפ מלא של מיקומי GPS (טסט ייעודי משווה את שני האלגוריתמים במפורש, לא רק "נראה טוב").

## הרצה — 17/17 טסטים עוברים

`src/features/parks/core/components/__tests__/turn-carousel-station-cue.test.ts`:

**רגרסיה (בלי תחנות) — 6 טסטים:** buildAllTurns זהה בדיוק להתנהגות הישנה; null/undefined/[] שקולים; כרטיס היעד לא השתנה; סדר וזהות-אובייקט של הפניות נשמרים; **סריקת GPS מלאה לאורך המסלול** (7 נקודות) — כל נקודה מניבה את אותו אינדקס בדיוק כמו האלגוריתם הישן שנשמר בקובץ הטסט כעוגן; מיפוי האייקונים לכל תווית תמרון אמיתית (ימינה קל/שמאלה קל/פנה ימינה/פנה שמאלה/יעד/ישר/ברירת-מחדל) זהה.

**תכונה חדשה (עם תחנות) — 11 טסטים:** תחנה בין שתי פניות נכנסת באמצע (לא בסוף); תחנה לפני הפנייה הראשונה נכנסת ראשונה; תחנה שנצמדת ל-vertex האחרון עדיין נשארת **לפני** כרטיס היעד (לא מתחרה/מתבלבלת איתו); כמה תחנות ממוינות נכון גם כשמועברות לא-לפי-סדר; שם חסר/רק-רווחים נופל לברירת מחדל "תחנה" בלי קריסה; קואורדינטות פגומות (NaN/undefined) מסוננות בשקט; `computeCurrentGpsIdx` מדגיש נכון את התחנה כש-GPS מתקרב אליה (לא קופץ ישר לפנייה הבאה); אייקון Dumbbell מוחזר לכל קלף עם `kind:'station'`, גם אם ה-instruction שלו מקרית תואם תווית-תמרון אמיתית (מוכיח שה-`kind` הוא הקובע, לא תוכן המחרוזת).

```
Test Files  1 passed (1)
     Tests  17 passed (17)
```

**vitest מלא (213 קבצים):** 209 עוברים, אותם 4 כשלונות ידועים-מראש ולא-קשורים (אמולטור Firestore לא רץ; 2 סקריפטי pre-vitest שטרם הוחרגו **על הענף הזה ספציפית** — התיקון קיים רק בענף התחנות, לא מוזג עדיין; test-isolation ב-logMultiCategoryWorkout) — כולם מתועדים בעבר, אף אחד לא נגרם משינוי כאן.

**tsc:** 445 שגיאות בענף כולו — **אפס** נוגעות ל-4 הקבצים ששיניתי.

## מה עדיין לא נבדק (מחוץ לתחום הטסטים האלה)

- **רינדור אמיתי במכשיר/דפדפן** — הטסטים מכסים את הלוגיקה הטהורה בלבד (אין jsdom בריפו הזה); איך זה *נראה* בפועל (צבע, גודל, "עוד 120מ' לתחנה 2" בזמן אמת) לא אומת ויזואלית. דורש בדיקה ידנית של דוד אחרי merge, בדיוק כמו שביקשת ל-48 שעות לפני ההשקה.
- **צבע הקלף** — נשאר `PRIMARY` (הכחול הרגיל של פנייה), לא צבע ייעודי לתחנה — בחירה מכוונת למזער סיכון (טוקן צבע חדש = עוד משתנה על רכיב חי ב-5 מסכים); ההבחנה מגיעה מהאייקון (Dumbbell) והכותרת (שם התחנה), לא מצבע. אם תרצה צבע ייעודי — שינוי קטן, נפרד.
- **מרחק סטטי לתחנה כש-GPS קר** (`distanceMeters:0`) — משפיע רק על חלון קצר לפני נעילת GPS; לא נבנה חישוב-מרחק-סטטי מדויק (מורכבות/סיכון לא הצדיקו את זה מול התועלת השולית).

**לא merge, לא push.**
