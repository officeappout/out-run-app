# עצירה: זהות "מסלול הכלניות" — לא מה שחשבנו — 22.09.2026

**אתה צדקת לעצור. הכל שנבדק עד עכשיו היה נגד מסלול שגם הוא, כפי שהתברר, מתויג "בדיקה" — לא רק ה-164 נק'.**

## 1. המסלול בן 164 הנק' — הוא לא הכלניות. גם הוא "בדיקה".

```
id: YVx4pgJjOOXR8j497pPF
name: "מסלול קלאסי שכונת הכרמים בדיקה"
```

❌ לא מכיל "כלני" בשם בכלל. **וגם השם שלו עצמו מכיל "בדיקה"** — כל מה שנבדק בעשרות המסמכים הקודמים (GPS tolerance, dedup, סדר-תחנות, תיקון הקריסה שהוכח עליו) היה מול מסלול-בדיקה, לא מול נתון-אמת מאומת. **חשוב: התיקונים בקוד עצמם (קריסת גיאומטריה, existing_route fallback, סינון פינים כפולים, TurnCarousel) הם גנריים לחלוטין — לא תלויים במסלול הספציפי הזה, ולא צריך לשכתב אותם.** מה שכן צריך אימות מחדש הוא רק הבדיקות הספציפיות-לנתונים (מרחקים, סדר, dedup) — בדיוק מה שביקשת.

## 2. שני מועמדים אמיתיים ל"כלניות" — ולא ברור איזה מהם

חיפוש "כלני" בכל 28 מסלולי שדרות מצא **שניים**:

| | "מסלול הכלניות בדיקה" | "כלניות" |
|---|---|---|
| id | `L3q3SY0UaCeJHdtHUOV7` | `LEr6UrEftKa5G4xefpHi` |
| נקודות | 88 | 74 |
| type / activityType | walking / walking (עקבי) | **running / walking (לא עקבי!)** |
| נוצר | **היום 08:21:10** | 30.03.2026 (ישן) |
| עודכן לאחרונה | היום 08:21:10 (=נוצר, לא נערך מאז) | **היום 08:16:27** |
| path[0] | lat=31.534048, lng=34.594476 | lat=31.533178, lng=34.599770 |

## ⚠️ ממצא קריטי מציר-הזמן: זה כנראה נתון-בדיקה שנוצר הרגע, לא נתון קיים

בדקתי את חותמות הזמן במדויק. הרצף המדויק (UTC, היום 22.09.2026):

```
08:16:27 — "כלניות" (המסלול הישן, ממרץ) עודכן
08:21:10 — "מסלול הכלניות בדיקה" נוצר (חדש לגמרי)
08:27:58 — הפארק שנקרא עד עכשיו "נאות השקמה" (W2BrOhXzngOSUOOsyNvx) שונה שם ל-"גינת כושר הכלניות בדיקה"
```

**שלוש עריכות רצופות, בפער של דקות, ממש עכשיו — כנראה בזמן שכתבת את ההודעה האחרונה.** זה מסביר את ה"בדיקה" בשם: **"מסלול הכלניות בדיקה" והפארק המתוייג-מחדש נראים כמו נתוני-בדיקה שהוכנו הרגע** (אולי על ידך, כדי לתת לי בסיס נקי ועקבי לבדוק מולו) — **לא נתון production מאומת**. **חשוב לציין:** זה שינה גם נתון אמיתי-קודם — הפארק שנקרא "נאות השקמה" בכל התיעוד הקודם (מסמכים 10-20) **כבר לא קיים תחת השם הזה** — הוא "גינת כושר הכלניות בדיקה" עכשיו.

**ההשערה שלי (לא ודאות, מבקש אישור שלך):** "**כלניות**" (`LEr6UrEftKa5G4xefpHi`) — קיים מאז מרץ, חלק מ-28 המסלולים המקוריים של שדרות — הוא **המועמד הסביר יותר להיות המסלול האמיתי** שהעירייה משלטת. "מסלול הכלניות בדיקה" נראה כמו scaffold זמני שהוכן הרגע לבדיקה נקייה (נקודות עגולות, תיוג עקבי, פארק-תמיכה משויך). **אך לא אניח את זה כעובדה — מחכה לאישורך.**

**רשימת 28 שמות המסלולים המלאה** נבדקה ידנית — אין שם נוסף שמזכיר "כלניות" בניסוח אחר (לא "אנמונות", לא מספר-מסלול). אם השם הרשמי שונה לגמרי ממה שחיפשתי — תגיד לי איך הוא כן נקרא.

## 3. הרצתי את כל הבדיקות מחדש — נגד "מסלול הכלניות בדיקה" (המועמד שכבר יש לו path/parks מוכנים)

⚠️ **כל התוצאות למטה הן נגד המועמד ה"בדיקה" — לא נגד "כלניות" (הישן). אם תאשר ש"כלניות" הוא האמיתי, אריץ הכל מחדש נגדו במקום.**

- **גיאומטריה:** 88 נק' (זוגי), תקינה, path[0]=(31.534048, 34.594476), 1.5 ק"מ, loop.
- **תחנת המתיחות (31.533280, 34.599028):** **2.8מ' בלבד** מהמסלול — בתוך 180מ' בנוח. (לשם השוואה: מול המסלול הישן-בטעות היה 256.7מ', מחוץ לטווח.)
- **הכרמים:** **358.5מ'** מ"מסלול הכלניות בדיקה" — **מחוץ ל-180מ', לא ייאסף כתחנה על המסלול הזה בכלל.**
- **נאות השקמה (בשמה הישן):** לא רלוונטי — הפארק כבר לא קיים בשם הזה (ראה §2), הוא עכשיו "גינת כושר הכלניות בדיקה" ונמצא **24מ'** מהמסלול הזה (strength).
- **resolveRouteStops בפועל:** אוסף 2 תחנות אמיתיות — "גינת כושר הכלניות בדיקה" (strength@24מ') ו-"אזור לתרגילי מתיחות" (stretch@3מ').
- **סבולת GPS ב-path[0]:** מרווח vertex מקומי **22.6מ'** (גס יותר מה-7-9מ' של המסלול הקודם). ב-20מ' סטייה entryIndex כבר זז מ-0 ל-1 — מרווח-בטיחות דק יותר מהקודם.
- **סדר מ-path[0]:** גינת כושר הכלניות בדיקה ← אזור לתרגילי מתיחות.

## 4. בדיקת existing_route בפועל — ממצא נוסף שדורש תשומת לב

הרצתי בדיוק את הלוגיקה האמיתית של `resolveRouteStopsBackbone('existing_route', ...)` (`start-hybrid-session.ts:646-676` — **חיפוש-מרחק גלובלי, בלי סינון-רשות בכלל**, מאומת בקוד) עם משתמש-מדומה העומד בדיוק ב-path[0] של "מסלול הכלניות בדיקה":

```
מנצח: "מסלול הכלניות בדיקה" @ 0.0מ' ✅ (צפוי — המשתמש עומד עליו)
שני הכי קרוב: "כלניות" @ 0.8מ' בלבד!
```

**שני המועמדים כמעט חופפים בנקודת ההתחלה (הפרש של פחות ממטר).** זו לא רק שאלה תיאורטית: אם "מסלול הכלניות בדיקה" יימחק (סביר, בהינתן שזה scaffold זמני) — `existing_route` "יזלוג" אוטומטית ל"כלניות" (74 נק', תיוג לא-עקבי running/walking) בלי שום שינוי קוד. שני המסלולים כנראה **חופפים גיאוגרפית באזור ההתחלה** — כדאי לבדוק את זה ביחד לפני שמחליטים על תיוג/מחיקה.

## 5. תיוג type — מאומת בקוד: לא משפיע על הבחירה בכלל

`resolveRouteStopsBackbone`'s `existing_route` branch לא בודק `type`/`activityType`/`activityTypes` בשום מקום — הבחירה 100% גיאומטרית (המרחק הקרוב ביותר לוורטקס בודד ברשת כולה). **אם "כלניות" יתברר כמסלול האמיתי, למרות שהוא מתויג `running` (עם `activityType:walking` לא-עקבי) — זה לא חוסם אותו מלהיבחר כ-backbone ל-route_stops.** לא נדרש שינוי קוד בשביל זה.

---

## מה אני צריך ממך כדי להמשיך

1. **איזה מסלול אמיתי:** "כלניות" (הישן, `LEr6UrEftKa5G4xefpHi`), "מסלול הכלניות בדיקה" (`L3q3SY0UaCeJHdtHUOV7`, כנראה scaffold-בדיקה שהוכן הרגע), או משהו שלישי שלא מצאתי?
2. אם "כלניות" — **אריץ את כל הבדיקות (§3) מחדש נגדו** (יש לו path משלו, 74 נק', path[0] שונה) ואדווח.
3. **הבהרה על הפארק שהוסב שם** ("נאות השקמה" ← "גינת כושר הכלניות בדיקה") — האם זה קבוע, או שינוי-בדיקה זמני שצריך להחזיר?

**עד אז: שום דבר לא merge, שום push, כמבוקש.**
