# Backlog item + בדיקת משתמש-חדש — 22.09.2026

## Backlog (פוסט-השקה, לא לפני)

**טבלת TIER נותנת 1-3 חזרות לכל תרגיל ב-levelDelta≥1 — צריך להבחין בין תרגילי מיומנות/עומס לתרגילי בסיס (בטן, תאומים).**
מקור: `workout-generator.types.ts:51-69` (`TIER_TABLE`/`resolveTier`), נצרך על ידי `assignVolume` (`workout-budgeting.utils.ts:544`). ליבת-מנוע רוחבית, לא hybrid-ספציפית. ר' מסמך 30 לפירוט מלא.

## בדיקת משתמש-חדש — READ-ONLY, סגור

**⚠️ כן — זה קורה גם למשתמש חדש לגמרי, בברירות מחדל. זה חוסם השקה.**

רמת-דומיין למשתמש חדש-לגמרי (לא הוערך) = **1** (קבוע קשיח, `workout-selection.utils.ts:683` — "when no domain-specific level exists... rawDomainLevel = 1").

בדקתי את שלושת התרגילים ישירות מול Firestore (`targetPrograms` האמיתי של כל אחד):

| תרגיל | id | רמת-תרגיל (`targetPrograms[].level`) | levelDelta (חדש−1) | tier | תוצאה |
|---|---|---|---|---|---|
| כפיפות בטן | `1OEFeykCym2378rgD4QK` | 2 | 1 | **hard** | **1-3 חזרות ⚠️** |
| עליות תאומים (בסיס) | `B0pnjy0oT9jTizikeLSb` | 1 | 0 | match | 3-6 חזרות (תקין) |
| עליות תאומים **על מדרגה** | `wfgHCel9MyopaIXRQS8D` | 2 | 1 | **hard** | **1-3 חזרות ⚠️** |

**ההודעה המקורית שלך נקטעה ב-"עליות תאומים על..." — כמעט ודאי "על מדרגה" (הגרסה שקופצת ל-hard). אם כך — שני התרגילים שראית בפועל (כפיפות בטן + עליות תאומים על מדרגה) יקפצו ל-1-3 חזרות לכל משתמש חדש-לגמרי, לא רק לחשבון-הבדיקה שלך.**

**המשמעות:** זה לא תלוי-פרופיל-ספציפי כפי שחשבתי — זה תלוי אך ורק ברמת-התרגיל המתויגת (2 ומעלה תמיד קופץ ל-hard/elite עבור כל משתמש-חדש, כי הרצפה הקשיחה היא תמיד 1). **כל תרגיל שמתויג `targetPrograms[].level ≥ 2` ונבחר לתחנה יציג 1-3 חזרות לכל תושב שמתחיל בלי היסטוריה — כולל ביום ההשקה.**

**עוצר כאן — זה חוסם השקה, לפי הקריטריון שהגדרת.**
