# Preview ב-Vercel מוכן — 22.09.2026

## 1. Push — בוצע

`feat/route-editor-freehand-satellite` נדחף ל-`origin` (לא ל-main, לא merge). Vercel זיהה את ה-push, בנה, ו-**הסתיים בהצלחה** (נבדק בפועל דרך GitHub Commit Status API, לא הנחה).

## 2. כתובות

**Preview (יציב, לא משתנה בין push עתידיים לאותו ענף):**
```
https://out-run-app-git-feat-route-edit-823464-davids-projects-0d6e1f97.vercel.app
```

⚠️ **חשוב:** ה-Preview הזה מוגן מאחורי Vercel Deployment Protection (SSO של הצוות) — בדקתי בפועל, הבקשה מפנה ל-`vercel.com/sso-api`. אם אתה מחובר בדפדפן לחשבון Vercel של הצוות (`davids-projects-0d6e1f97`) זה יעבור אוטומטית; אם לא — תתבקש להתחבר ל-Vercel פעם אחת.

**עורך המסלולים שנבנה (ריק, למסלול חדש):**
```
/admin/routes/new
```
כלומר בפרודקשן: `<preview-url>/admin/routes/new`

**⚠️ ממצא חשוב — "מסלול הכלניות בדיקה" לא ניתן לטעינה בכלי הזה בכלל.** בדקתי: `RouteEditor.tsx` (הכלי ששיניתי) מחובר רק לשני מסכי **יצירת מסלול חדש** — `/admin/routes/new` ו-`/admin/authority/routes/new` — שניהם תמיד מתחילים מקנבס ריק, בלי אפשרות לטעון מסלול קיים לעריכה. **עריכת מסלול קיים (כמו "מסלול הכלניות בדיקה") עוברת דרך מערכת נפרדת לגמרי:**
```
/admin/authority/routes/L3q3SY0UaCeJHdtHUOV7/edit
```
זה קובץ אחר (`route-geometry-edit.service.ts`, מנוע עריכה-בטוחה עם bridge-connectors) — **לא נגעתי בו, אין לו לוויין/ציור-חופשי.**

**המשמעות המעשית:** ב-Preview הזה תוכל לבדוק את לוויין+ציור-חופשי רק על-ידי **ציור מסלול חדש** (אולי מעל "מסלול הכלניות בדיקה" הקיים, כדי להשוות ויזואלית) — לא עריכה-במקום של הרשומה הקיימת. אם התוכנית היא לתקן את "מסלול הכלניות בדיקה" עצמו במקום, זה ידרוש עבודה נוספת (לחבר בין שני העורכים, או להוסיף את היכולות ל-`[id]/edit`) — לא בתחום מה שנבנה עד עכשיו.

## 3. Firebase Auth — נבדק בפועל, לא רק תיאוריה

הרצתי קריאת-קריאה-בלבד מול ה-Identity Toolkit Admin API (לא Firestore — הגדרת-Auth ברמת-פרויקט, לא נגישה דרך firebase-admin הרגיל, אז כתבתי סקריפט ייעודי עם OAuth token מה-service account). **הרשימה בפועל היום (5 דומיינים):**
```
localhost
appout-1.firebaseapp.com
appout-1.web.app
out-run-app.vercel.app   ← זה דומיין הפרודקשן, לא זהה ל-preview!
outrun.co.il
```

**התחברות ב-Preview הזה תיכשל** (`auth/unauthorized-domain`) — `out-run-app.vercel.app` ≠ `out-run-app-git-feat-route-edit-823464-davids-projects-0d6e1f97.vercel.app`, מחרוזות שונות, Firebase לא עושה wildcard-match.

**מה להוסיף, בדיוק, ואיפה:**
1. Firebase Console → הפרויקט `appout-1` → Authentication → Settings → Authorized domains → Add domain.
2. הוסף בדיוק: `out-run-app-git-feat-route-edit-823464-davids-projects-0d6e1f97.vercel.app`

**לא נדרש בשביל לבדוק את הכלי עצמו** (ציור/לוויין/שמירה-לא-בפועל) — רק אם תרצה להתחבר לחשבון בתוך ה-Preview הזה.

## 4. רשימת בדיקה קצרה

| מיקום | פעולה | מה אמור לקרות |
|---|---|---|
| סרגל צד, אחרי "סוג פעילות" | לחיצה על "הצמדה לכביש" / "ציור חופשי" | מחליף בין ניתוב-אמיתי (Directions API, "מנתב..." מופיע רגע) לקו-ישר-מיידי (בלי המתנה) |
| מפה, קליק | הוספת נקודה | בשני המצבים: פין חדש (A/מספר/B), קו מתעדכן |
| מעבר בין מצבים **אחרי** שכבר יש נקודות | לחיצה על המתג השני | הקו כולו נבנה-מחדש בהתאם למצב החדש (לא רק נקודות עתידיות) |
| מפה, פינה ימנית-תחתונה | "תצוגת לוויין" / "תצוגה רגילה" | המפה עצמה מתחלפת; שמות-רחובות בעברית נשארים קריאים בשני המצבים |
| סרגל עליון | "סגור מעגל" (פעיל מ-3 נקודות) | נקודה זהה לראשונה נוספת בסוף, הקו נסגר למעגל (מכבד את מצב ההצמדה הנוכחי) |
| סרגל צד, "סיכום" | — | "נקודות לשמירה" — במצב חופשי המספר **גדול** ממספר הקליקים בפועל (צפיפות אוטומטית); אזהרה כתומה אם אי-זוגי |
| סרגל עליון | "ביטול נקודה" / "נקה הכל" | מוחק בבטחה — מצב מקומי בלבד, שום קריאת רשת/שמירה |

**⚠️ אל תלחץ "שמור מסלול"** — זה כותב מיד ל-`official_routes` בפרודקשן אמיתי (`published:true`, גלוי לכל משתמש קצה תוך שניות). כל שאר הפעולות למעלה — כולל "סגור מעגל" (כן קורא ל-Mapbox Directions API בפועל, אבל לא כותב לשום מקום) — בטוחות לגמרי לבדיקה חופשית.
