"use client";

/**
 * @deprecated Use `@/features/navigation/BottomNavbar` instead.
 * This component is kept for backwards compatibility but should not be used directly.
 * The canonical bottom navigation is rendered globally by ClientLayout.
 */

import BottomNavigation from '@/features/navigation/BottomNavbar';
export default BottomNavigation;
