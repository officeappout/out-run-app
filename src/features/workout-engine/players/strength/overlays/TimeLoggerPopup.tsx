'use client';

/**
 * TimeLoggerPopup
 * Overlay component for logging time-based exercise metrics
 * Used for timed exercises (e.g., planks, holds)
 */

import React from 'react';

interface TimeLoggerPopupProps {
  // Props will be defined in future implementation
}

export default function TimeLoggerPopup(props: TimeLoggerPopupProps) {
  // Placeholder implementation
  return (
    <div>
      {/* TimeLoggerPopup component - Implementation coming soon */}
    </div>
  );
}
