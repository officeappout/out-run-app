'use client';

// Phase 1.5 — stubbed. Returns null until PR detection query is implemented.
// When implemented: renders an amber banner (#E0A33E) for broken records.
export default function PersonalRecordTag() {
  return null;
}
