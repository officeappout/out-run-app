/**
 * Parks Feature Barrel Export (Master)
 * Unified access to all parks functionality
 */

// Re-export everything from core (most commonly used)
export * from './core';

// Re-export client components
export * from './client';

// Admin exports (currently empty, but structured for future use)
export * from './admin';
