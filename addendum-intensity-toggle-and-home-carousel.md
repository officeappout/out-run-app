# תוספת למסמך fix-round-consolidated-19-08.md — מעבר בורר-עוצמה לדף האימון + קרוסלה אמיתית בבית

**סטטוס:** סעיף חדש, סוכם עם דוד ב-21.08.2026. הערת שקיפות: העותק המקומי שלי של המסמך המלא אבד באיפוס סביבת העבודה — זו תוספת עצמאית, מיועדת להתווסף/להישלח יחד עם המסמך המקורי שכבר יש לך ולקורסר, לא תחליף לו.

## הרקע (מה שדוד הסביר)

הבורר של "פיל קטן שבלחיצה פותח מגירה עם 3 האפשרויות" (`StatsOverview.tsx`, `isIntensitySheetOpen`, במסלול `HOME_ANCHOR_V2_ENABLED`) היה **פתרון זמני** — לא העיצוב הסופי. הכוונה המקורית: לצמצם מ-3 כרטיסים בקרוסלה לבחירה אחת מוצגת, וקורסר בחר את מנגנון הפיל+מגירה בתור פתרון ביניים. **זה לא הכיוון הנכון בטווח הארוך.**

## מה שצריך לקרות בפועל — 2 חלקים

### חלק א׳ — 3 האפשרויות עוברות לדף האימון עצמו, כטוגלים

**היעד הוא לשכפל מנגנון קיים, לא להמציא חדש.** נמצא ומאומת בקוד:

`src/features/parks/core/components/hybrid/HybridOverviewScreen.tsx`, שורות 507-535 — שורת טוגלים אחת (`role="group"`, `aria-label="בחירת עצימות"`), 3 כפתורים צמודים (`flex-1`), כל אחד עם `DifficultyBolts` + תווית (קליל/מאוזן/עוצמתי) + דקות, מצב נבחר מודגש ישירות בשורה (`active ? STR_TINT : 'transparent'`), נשלט ע"י `selectBolt(i)` / `boltIndex`. **בלי מגירה, בלי bottom sheet — הבחירה קורית inline.**

**המשימה:** לשכפל את דפוס העיצוב הזה (לא להעתיק קוד מילולית — `HybridOverviewScreen` הוא הקשר park/hybrid שונה) לתוך דף האימון שנפתח מהבית (המסך שהמשתמש מגיע אליו אחרי בחירת אימון מה-hero card של `StatsOverview.tsx`). קורסר צריך קודם לזהות בדיוק איזה קומפוננטה/מסך זה (כנראה workout-preview-drawer או מסך תצוגה מקדימה של האימון — **לא ידוע לי איזה קובץ בדיוק, קורסר צריך לאתר ולדווח לפני שכותב קוד**, לפי חוק "audit-before-code" של הפרויקט).

**מה שיורד:** הפיל + `isIntensitySheetOpen` + חלונית ה-bottom-sheet (`AnchorOptionToggles`) ב-`StatsOverview.tsx` — כל המנגנון הזמני הזה מוסר לגמרי מהבית.

### חלק ב׳ — אזור הבית הופך לקרוסלה אמיתית (להחלקה לצדדים)

כרגע (`StatsOverview.tsx`, המסלול `HOME_ANCHOR_V2_ENABLED`) האזור הזה הוא `flex flex-col` — טור אנכי קבוע: פיל → כרטיס hero בודד → כפתור "בנה בעצמך" → (ביום מנוחה בלבד) כרטיס הליכה נפרד למטה. **אין שום קרוסלה אופקית קיימת כרגע בנתיב הזה.**

**היעד:** ברגע שחלק א׳ מוציא את בורר-העוצמה מהאזור הזה, האזור מתפנה להיות קרוסלה אמיתית להחלקה לצדדים (horizontal scroll), עם כרטיסי הצעות — כרטיס ההליכה למילוי-פער-צעדים (`RouteCardUnified`, כרגע גם ככה מוגבל ל-`trioResult.isRestDay`, שורה ~1115-1136) הופך מבלוק קבוע-למטה לפריט אחד בתוך הקרוסלה הזו. **חשוב: זה צריך לעבוד באותה צורה בלי קשר למצב — לפני אימון, אחרי אימון, וביום מנוחה — לא 3 התנהגויות שונות.** כרגע לא בטוח אילו כרטיסים נוספים (חוץ מ-hero + הליכה) קיימים כבר בקוד ומתאימים להיכנס לקרוסלה הזו — **קורסר צריך לסקור זאת ולדווח לפני מימוש**, לא להניח.

## הערת סדר-גודל (לתשומת דוד, לא לקורסר)

זו לא משימת CSS קטנה — זה שינוי מבני בשני מקומות שונים (בורר-עוצמה עובר בין מסכים, ואזור שלם בבית עובר ממבנה טור-קבוע למנגנון קרוסלה). כדאי לתת לקורסר לפרק את זה לשני commits נפרדים (חלק א׳, חלק ב׳), לא commit אחד ענק.

---

## פרומט לקורסר

**"לפני כתיבת קוד — תסקור ותדווח (לא לכתוב עדיין): (1) איזה קומפוננטה/מסך נפתח מהבית כשבוחרים אימון (ה-'דף אימון' שאליו 3 האפשרויות צריכות לעבור), (2) אילו כרטיסי-הצעה כבר קיימים בקוד שיכולים להיכנס לקרוסלה החדשה בבית חוץ מ-hero card וכרטיס ההליכה. אחרי שאתה מדווח ואני מאשר — שני commits נפרדים: (א) העברת 3 האפשרויות (קל/בינוני/קשה) מהפיל+מגירה הזמניים ב-StatsOverview.tsx לטוגלים inline בדף האימון, לפי דפוס העיצוב ב-HybridOverviewScreen.tsx שורות 507-535 (לשכפל את הדפוס, לא להעתיק מילולית — הקשר שונה). מסירים את isIntensitySheetOpen וה-bottom-sheet מ-StatsOverview.tsx. (ב) הפיכת האזור המתפנה בבית לקרוסלה אופקית אמיתית (להחלקה לצדדים) — כרטיס ההליכה למילוי-צעדים (RouteCardUnified, שורות ~1115-1136) הופך מבלוק-קבוע-למטה לפריט בקרוסלה, ועובד באותה צורה לפני/אחרי אימון/יום מנוחה, לא 3 התנהגויות נפרדות."**
