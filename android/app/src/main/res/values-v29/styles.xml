<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      API 29 (Android 10) and above.

      Inherits the same edge-to-edge transparent system bars as the base
      values/styles.xml. The android:navigationBarContrastEnforced=false
      override that previously lived here was removed because it broke
      the build on toolchains that ship an older platform jar. Without
      it, Android 10+ MAY apply a faint semi-opaque scrim behind a
      transparent navigation bar — acceptable trade-off in exchange
      for an unblocked build.

      This file is kept (rather than deleted) so future API-29-only
      overrides have an obvious home.
    -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowTranslucentNavigation">false</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>
