# Runtime config for Cloud Functions in this codebase (functions/) — loaded automatically by
# the Firebase Functions v2 runtime at deploy time and in emulation. NOT the same mechanism as
# the repo-root .env.local (gitignored, never deployed) — this file IS meant to be committed.
#
# NEXT_PUBLIC_MAPBOX_TOKEN — required by the vendored geo-discovery-routes.ts (functions/src/
# _vendor/scripts/geo-discovery-routes.ts) for Mapbox Terrain-RGB DEM elevation tiles
# (loadTiles()). Safe to commit: this is the same NEXT_PUBLIC_* token already inlined into the
# public browser bundle on multiple admin pages (src/app/admin/locations/page.tsx and others) —
# reusing it here introduces no new exposure, it is already public.
NEXT_PUBLIC_MAPBOX_TOKEN=pk.eyJ1IjoiZGF2aWQtb3V0IiwiYSI6ImNtanZpZmJ0djM5MTEzZXF5YXNmcm9zNGwifQ.8MD8s4TZOr0WYYgEpFfpzw
