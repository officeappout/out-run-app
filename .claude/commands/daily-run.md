# /daily-run — הפעלת סריקת CRM יומית

הפעל את הסריקה היומית של ה-CRM:

1. קרא את `.claude/skills/daily-authority-scan/SKILL.md` ובצע אותו.
2. אם המשתמש הוסיף `--dry` — העבר `dryRun: true` ל-API.
3. הצג את סיכום הבוקר בפורמט המוגדר בסקיל.
