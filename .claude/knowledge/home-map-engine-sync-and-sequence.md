# Home+Map Decision-Engine — Sync Check + Proposed Build Sequence

> נכתב: 15.08.2026, בעקבות בקשת-סנכרון של דוד (מצא שרשור-דרייב עשיר שלא קיבל התייחסות + פער בסטטוס-פוש של feat/home-daily-goal-v1). מסמך זה = תוצר-הסנכרון המלא: שורש-הפער, אימות-קוד טרי, ומיפוי כל מסמכי-הדרייב הרלוונטיים למקום אחד.
> קרא לפני כל עבודה על: כרטיסי-מפה/היברידי, מנוע-הצעות, feat/home-daily-goal-v1, A2 (טיימר/GPS).
> PLACEHOLDER: אין.

---

## חלק א' — שורש פער-המידע (למה זה קרה)

**הטענה שנבדקה:** אמרתי לדוד ש-`feat/home-daily-goal-v1` "מעולם לא נדחף" — שגוי. הענף כן קיים ב-`origin` (`8b42fca1`, נכון ל-15.08: 448 קומיטים מאחורי main / 37 קדימה).

**שורש מאומת (מקור: מסמך "מצב-אמת מאוחד", דרייב, 7.8.2026, חלק ג'):**
הענף התגלה 7.8.2026 כקיים **רק מקומית** (אפס עדות ב-GitHub), ונדחף מיידית כפעולת-חירום לשימור. קובץ-הזיכרון המקומי שלי (`home-daily-goal-v1.md`) נכתב 25.7.2026 — **לפני** אירוע-הגיבוי הזה, ולכן היה נכון-כשנכתב והתיישן בלי שאף אחד עדכן אותו. הטעות שלי: כשקראתי אותו וחזרתי על הניסוח, לא הצלבתי אותו מול בדיקת-git טרייה שכבר ביצעתי **באותה פנייה עצמה** (שכבר הראתה שהענף קיים ב-origin). כשל בהצלבה, לא אובדן-מידע.

**דוגמה נוספת, בלתי-תלויה, לאותה מחלת-יסוד — נמצאה תוך-כדי הסנכרון הזה:** מסמך "מצב-אמת מאוחד" עצמו מתעד מקרה-זהה מ-7.8: "גילוי חשוב באגב: מנוע-ההצעות + ניתוב-לשאלון-סקילים... התברר כבר-בנוי-וחי מ-PR #29 — המסמך הישן לא עודכן אחרי שזה נבנה. **לקח: לא לסמוך על תיעוד-סטטוס בלי לאמת מול הקוד החי.**" — זה בדיוק אותו באג-תהליכי, שקרה גם לצ'אט אחר, לא רק לי. וגם A2 (חלק ב' למטה) התגלה כאותה תבנית: התוכנית המקומית (`happy-jumping-flask.md`) תיעדה סטטוס אחד, אבל רק בדיקת-git ישירה חשפה את התמונה המלאה (תוקן, אז הוחזר).

**מסקנה מעשית:** מעכשיו, לפני כל דיווח-סטטוס על סעיף ישן — git-check ישיר קודם, לא רק שליפת-זיכרון. הבדיקות בחלק ב'-ד' למטה **כולן** בוצעו ככה (לא מהזיכרון/מהמסמכים בלבד).

---

## חלק ב' — פערי-האותיות (A2, C1, C2, C4)

### A2 — split-brain טיימר/GPS בריצה — ✅ **סגור (15.08.2026) — לא באג, החלטת-מוצר**

**תשובת דוד:** לא מאפשרים לצאת ממסך המפה באמצע ריצה חיה — ולכן אין תרחיש-GPS-ברקע לתחזק. ה-revert (`93e78fcb`, 12.8) לא היה רגרסיה שנתקלו-בה — הוא נבע ישירות מההחלטה הזו (ברגע שיציאה-מהמסך חסומה מוצרית, התיקון שפותר "GPS ממשיך לרוץ אחרי יציאה" הופך למיותר). **סגור, אין המשך-טיפול.**

**סטטוס-שרשרת, מאומת ישירות מול `origin/main`:**

| שלב | Commit | תאריך | סטטוס |
|---|---|---|---|
| אבחון-שורש (spike) | `26d5c43b`, `a681564f` | 11.8 | ✅ בוצע — רץ בפועל, לא "לא התחיל" |
| תיקון אמיתי | `e245519b` fix(gps): decouple GPS acquisition from /map route lifecycle | 11.8 | ✅ נבנה ומוזג ל-main |
| **Revert** | `93e78fcb` Revert "fix(gps)..." | 12.8 | ✅ מוזג ל-main — **בעקבות ההחלטה למעלה, לא רגרסיה** |

⚠️ **פתוח:** דוד ציין גם commit שלישי לשלב-האבחון, `04165fd4` — **לא הצלחתי לאמת שהוא קיים** (`git cat-file -e` מחזיר "Not a valid object name", גם אחרי fetch מלא ובדיקת-commits-דאנגלינג). ייתכן טעות-הקלדה בהאש. שני ה-commits האחרים (`26d5c43b`, `a681564f`) כן מאומתים במדויק. לא ממציא איזה קומיט זה היה — מסמן כפער-קטן-פתוח בלבד.

שורש-הבאג ההיסטורי (מהודעת ה-commit של `e245519b`, לתיעוד בלבד — לא-רלוונטי-מעשית יותר): `useGPS()` היה כבוי-לגמרי ל-lifecycle של `MapShellInner`, בעוד הטיימר store-owned ועצמאי — יציאה מ-`/map` באמצע ריצה הייתה הורגת GPS בזמן שהטיימר ממשיך. עכשיו לא רלוונטי: היציאה עצמה חסומה מוצרית.

---

### C1-א (scheduledProgramIds gate) — ✅ **נדחה (15.08.2026)**

התסמין האמיתי של "וידאו-התאוששות לא נפתח" לא היה "רנדומליות-תוכן" כמו שהונח במקור — היה פער-שדה אמיתי (`previewVideo` מול `fullTutorial`) על 7 מסמכי Firestore, תוקן כ-data-patch (commit `7fc00480`, ראה גם C1-ב למטה). **דוד: נדחה גם באופן עצמאי** — עדיין אין מגוון-סרטונים שמצדיק סינון-לפי-תוכנית (כל 7 הסרטונים הקיימים מתאימים כרגע לכל התוכניות). שני נימוקים מתכנסים לאותה מסקנה: לא לבנות.

### C1-ב (admin upload widget כותב לשדה הלא-נכון) — 🔧 **בטיפול (15.08.2026)**

**דוד: לתקן עכשיו, לפני שמעלים סרטון הבא.** אותו דפוס-באג בדיוק כמו C1 (widget-ההעלאה סביר שכותב רק ל-`previewVideo`, לא גם ל-`fullTutorial`) — הפעם בקוד-ההעלאה עצמו, לא בסקריפט-הזריעה. ראו דיווח נפרד להלן לסטטוס-התיקון + דיף.

### C2 (ריצה → סרטוני-התאוששות) — ✅ **נדחה (15.08.2026)**

**דוד: לא רלוונטי כרגע.** נשאר מתועד לעתיד — אם וכאשר.

*תרחיש שנשאר רלוונטי-לעתיד:* משתמש שנמצא ביום-מנוחה ובוחר ריצה/הליכה חופשית במקום הטריו-וידאו — אין לו נתיב לתוכן-ההתאוששות מצד הריצה; קיים ונגיש רק מצד הבית/הכוח.

### C4 (תיוג "mobility" מת) — ✅ **נשאר כמו שהוא (15.08.2026) — לא תיקון, לא מחיקה**

**החלטת דוד:** לא בונים, לא מוחקים. אין עדיין תוכנית לתוכן גמישות/מתיחות, אבל ייתכן בעתיד (סרטוני-תחזוקה/מתיחות, אולי follow-along בסגנון-דומה ל-C1). פשוט לא נוגעים — קוד מת, אפס דחיפות, אבל שמור למקרה שהתוכן הזה יבוא.

---

## חלק ג' — כרטיס-אירובי: אימות גייט-הרמה ב"אימון משולב" (הבקשה המרכזית שלך)

**שאלה מקורית:** האם באג "רמה מומצאת=1 בלי gate" בכרטיס "אימון משולב" **באמת** תוקן — לא רק נראה-ככה ממסמכים.

**מתודולוגיה:** לא הסתמכתי על המסמכים בלבד. הרצתי חקירת-קוד עצמאית וטרייה נגד `origin/main` הנוכחי (15.08.2026, לא ה-snapshot של 6-7.8), עם ציטוט file:line ישיר. שלושה מקורות מתכנסים לאותה מסקנה:

### ✅ פסק-דין: FIXED-AND-VERIFIED-ON-MAIN

**המנגנון בפועל, כפי שקיים היום:**
```ts
// src/features/user/identity/services/access-control.service.ts:153-158
export function hasAssessedStrengthDomain(user: UserFullProfile | null): boolean {
  const NON_STRENGTH = new Set(['running', 'flexibility']);
  const anyAssessed = (obj) => Object.entries(obj).some(([k, v]) => !NON_STRENGTH.has(k) && readLvl(v) > 0);
  return anyAssessed(user?.progression?.domains) || anyAssessed(user?.progression?.tracks);
}
```
נקרא מ-3 אתרים עצמאיים ב-`start-hybrid-session.ts` — `composeFullParkWorkout` (:327), `composeRouteStopsWorkout` (:712, :738), **ו-`composeHybridPlan`** (:1142, ה"אימון משולב" עצמו — קומנט בקוד ב-1106-1109 מאשר זו "הנקודה היחידה ששני נקודות-הכניסה החיות ל-budget-split branch נוחתות בה"). משתמש-לא-מוערך ממשיך בשקט **רק** אם הפארק-שנמצא הוא הידראולי-בלבד (תואם עיקרון-2 מעץ-ההחלטות: הידראולי=יוצא-דופן-בטוח) — אחרת מקבל session אמיתי עם `fallbackHint`/`assessmentDomains` ואפס-תחנות. **לא רמה מומצאת, אף פעם.** שני הדגלים המפעילים = `true` ב-main (`feature-flags.ts:70,90`) — חי, לא כבוי.

**תיקון להנחה המקורית שלי:** חשבתי ש-3 commits (`6608c00`+`d4c2385`+`9b7fe1b`) הם "התיקון + 2 המשכים". בפועל: `6608c00` = התיקון האמיתי. `d4c2385` = הפיכת הודעת-החסימה לקישור-פעיל (עיקרון-4). `9b7fe1b` = איחוד-עיצוב בלבד, לא נוגע בגייט. `055a933` = **לא קשור בכלל** — תיקון-רקע-דולף ב-JITSetupModal (שאלון-בריאות), לא בהיברידי.

**ממצא חדש, לא ידוע קודם:** הגייט חוזק **עוד 8 commits** בין 6-10.8 (`3295e181`, `500276ec`, `ce0b70f9`, `41d6f1ff`, `ae010af1`, `29e38073`, `189c81f6`, `82c2f624`, `8d3ffb9d`) — כולם על main, אף אחד לא החליש. בולטים: `500276ec` שדרג את הגייט מ-`return null` שקט להחזרת session אמיתי עם הודעה גלויה-למשתמש; `ce0b70f9` איחד את התבנית ל-`buildNeedsAssessmentFallback()`; `8d3ffb9d` הוסיף gate מוקדם-אף-יותר ברמת-ה-slot (`STRENGTH_ASSESSMENT_PROMPT_CARD_V1=true` ב-main).

**✅ עדכון 15.08.2026 (re-verification מלא, לפי בקשת דוד): הליד הבא נסגר, לא נשאר פתוח.** הגייט בודק "יש בכלל תחום-כוח מוערך" — לא per-domain (בחירה מכוונת, תואמת את גייט ה-hero הקיים בבית). נבדק במלואו: 4 אתרי-צריכה נגד ה-sentinel `UNASSESSED_DOMAIN_LEVEL` (`-Infinity`) — כולם דוחים נכון (ContextualEngine tolerance-filter/skill-gate/scoring, compose-hybrid-session band-check). **אבל נמצא ממצא אמיתי, לא-ידוע-קודם:** `composeFullParkWorkout`'s תת-נתיב `full_body`-master (דרך `generateHomeWorkoutTrio`→`resolveAggregateFullBodyBudget`, `lead-program.service.ts:283-361`) **לא** משתמש ב-sentinel בכלל — יש שם `level=1` קשיח + `levelSource:'default(1)'` כש-resolution נכשל. **זו מוקש-רדום, לא באג-חי**: מגיע-אליו רק כש-`isFullBodyMaster===true`, שדורש ש-4 תחומי-הבן כבר-מוערכים-בפועל (נבדק ב-gate **נפרד ועצמאי**, `resolveChildDomainsForParent`) — כך שבזמן שה-branch-הבעייתי רץ, רמות-אמיתיות כבר קיימות. מוגן רק ע"י gate-עליון שהוא-עצמו לא-אוכף — אם ה-gate-העליון ישתנה בעתיד (או ייקרא ישירות בלי הפילטר), זו תהיה מימוש-שלישי-לא-מתוקן של אותו באג-היסטורי. **המלצה: פתיחת ticket קטן ונפרד להקשחה (sentinel או assertion), לא-דחוף, לא חוסם.**

### ✅ עדכון 15.08.2026 (בדיקת-המשך אוטונומית): תרחישים 5-6/8 מאושרים בפועל תוקנו — 9א/9ב לא

הליד למעלה (שהיה "לא מאומת, סביר") **נבדק ישירות מול diff מלא של `500276ec`, ומאושר:**

`fix(workout-engine/hybrid): the 3 unassessed-domain gates return a real session with fallbackHint/assessmentDomains instead of null` (8.8.2026, על main). לפני זה: `composeFullParkWorkout`, `composeRouteStopsWorkout`, ו-`composeHybridPlan`'s default branch כולם עשו `return null` כשמשתמש-לא-מוערך נתקל בפארק לא-הידראולי — והקורא (`composeAndShowOverview`/`composeTrioDeduped`) בונה `ctx` inline ולעולם לא שומר אותו, אז `stopGateReason` לא ניתן-לקריאה בשום מקום. **זה בדיוק תרחישים 5-6 (פונקציונלי-בלבד) ותרחיש 8 (רק-דשא, גם-הוא "אין הידראולי לבדוק, אז חוסם ישירות" לפי קומנט-הקוד).** אחרי-התיקון: מוחזר session אמיתי עם `fallbackHint`/`assessmentDomains`, באמצעות אותה תשתית-מוכחת שכבר-מרונדרת נכון היום (`HybridOverviewScreen.tsx:452-470`, `DiscoverLayer.tsx:1460-1463`'s `onAssessmentLink`) — לא UI חדש, לא ניחוש. מאומת: TSC=baseline (0 חדש), 67/67 טסטים אמיתיים ב-hybrid suite, `full-park-needs-assessment.test.ts` (5/5) אומת-מחדש ספציפית. **תרחיש-`route_stops` תוקן-לעקביות אך dark** (`MAP_ROUTE_STOPS_V1=false`) — full-park וה-default-branch כן חיים.

**9א/9ב נשארים ❌, לא נגעו:** זה תיקון לגייט-אחרי-שפארק-כבר-נבחר, לא לוגיקת-בחירת-פארק-בין-כמה-מועמדים. שום דבר ב-`500276ec` לא נוגע ב"איזה פארק נמצא ראשון כשיש גם דשא וגם פארק-מצויד" — התרחיש הזה עדיין תלוי-מזל, כמתועד.

**מסקנה מתוקנת לעיקרון-4 (טבלה למטה) ולתרחישים 5-6/8:** לשדרג מ-⚠️/❌ ל-✅ — לא צריך בדיקה נוספת, זה חי ומאומת.

**תרחיש-משתמש (למה זה משנה בפועל):** משתמש חדש שלא מילא שאלון, מגיע לפארק עם ציוד פונקציונלי (מתח/מקבילים, לא הידראולי) — **כיום** (לא רק "לפי 7.8") מקבל הודעה אמיתית עם קישור-לשאלון, לא כישלון-שקט. זה כבר-עובד, לא צריך לבנות.

### ✅ 15.08.2026 — Re-verification מלא לפי בקשת דוד, כולל "הנתיב הספציפי שהכרטיס-האירובי ישתמש בו"

**זיהוי "הכרטיס האירובי":** `FreeRunDrawer.tsx` — הכותרת בקוד עצמו: `"אירובי חופשי"` (שורה 1269). זה **בדיוק** אותו-רכיב שנרמז בעיקרון 6 ("אימון-אירובי-חופשי-בלי-כוח תמיד זמין"). יש לו 2 מצבים:
- **ברירת-מחדל (הטוגל כבוי):** `EntryButtons`→`handleStartFree`/`handleStartWithRoute` — נוגעים רק ב-`useRunningPlayer`+`route-generator.service`, **אף פעם לא** מייבאים את פונקציות-ההרכבה-ההיברידיות. מבנית לא-מסוגל להמציא רמת-כוח, כי הוא לא מייצר תוכן-כוח בכלל. מאומת ישירות, לא הונח.
- **טוגל דלוק** (`StrengthStationsToggle`, "💪 תחנות כוח בדרך") — קומנט-בקוד: "Presentational only — no engine coupling". בונה `HybridStartIntent` בלי `mode`, מנותב ל-`composeAndShowOverview`→**בדיוק** ל-`composeHybridPlan`'s default-branch (`start-hybrid-session.ts:1106-1109` מאשר זו "הנקודה היחידה ששני-נקודות-הכניסה-החיות נוחתות בה" — כרוסל-הבית וה-FreeRunDrawer-toggle, אותו קוד בדיוק). כלומר: **אין נתיב-נפרד-לא-מאומת** — הטוגל *הוא* אחת משתי הכניסות לגייט שכבר אומת.

**עקבים-קוד (מאומת מחדש, לא רק צוטט מהמסמכים — 2 מיקומים התגלו כ-line-drift, מנגנון זהה):**
- `HybridOverviewScreen.tsx` (קישור-לשאלון פעיל) — **:466-481 בפועל** (לא 452-470 כפי שצוטט קודם).
- `DiscoverLayer.tsx`'s `onAssessmentLink` — **:1719-1723 בפועל** (לא 1460-1463). `startMiniDomainAssessment` מאומת כניווט אמיתי (זורע sessionStorage, מנתב ל-`/onboarding-new/assessment-visual`, עם מנגנון-חזרה) — לא stub.
- בדיקת-רגרסיה מ-15.8 עד היום: 3 commits ב-`DiscoverLayer.tsx` היום — מאומתים קוסמטיים-בלבד (מיקום-פס-חיפוש, חפיפת-כפתור-שכבות, קיפול-פס-עליון), אפס-נגיעה בגייט/assessmentDomains/onAssessmentLink.

**מסקנה:** אור-ירוק לתחילת עבודת ה-UI על הכרטיס-האירובי. הטוגל-כבוי לא-צריך-גייט (טהור-מבנית). הטוגל-דלוק יורש את הגייט-המחוזק-הקיים. פריט-המשך יחיד, לא-חוסם: ticket להקשחת `resolveAggregateFullBodyBudget` (ראה למעלה).

### טבלת-מצב מלאה — 9 עקרונות (מאומת קוד, 7.8.2026, לפני 8 ה-commits הנוספים)

| # | עיקרון | סטטוס |
|---|---|---|
| 1 | בלי ניחוש-רמה | ✅ מוצק — UI-gate + engine-backstop, אין פער (וגם: מאומת-שוב היום, ראה למעלה) |
| 2 | הידראולי=יוצא-דופן | ⚠️ עובד רק ב"הליכה+כוח", לא ב"אימון מלא בפארק" |
| 3 | פונקציונלי דורש שאלון חוץ ממתחילים | ⚠️ חצי-חי; חריגת-מתחילים לא נבנתה כלל |
| 4 | כל חסימה=פעולה-אמיתית | ⚠️→✅ (עדכון 15.8) מאומת-תוקן ל-3 נתיבי-ההרכבה העיקריים (`500276ec`, 8.8) — ראה "עדכון 15.08.2026" למעלה. ייתכן שנשארו בלוקים אחרים לא-נבדקים מחוץ ל-3 אלה |
| 5 | רמות-גבוהות→פארק לא חסימה | ❌ אפס לוגיקת-סף-רמה בכל נתיבי-ההרכבה |
| 6 | אירובי-חופשי תמיד-זמין | ✅ ללא תנאי |
| 7 | כרטיס-מומלץ תמיד-ראשון | ✅ מאומת |
| 8 | מסלול-עצירות=פאזל | 🟡 בנוי, מאחורי `MAP_ROUTE_STOPS_V1=false` |
| 9 | שאלון-חלקי≠חסימה | ✅ מאומת |

**16 תרחישים — הכי-חשוב מהם:** 5-6/8 = ✅ **תוקן ומאומת** (`500276ec`, 8.8 — ראה "עדכון 15.08.2026" למעלה, לא צריך בדיקה נוספת). 9א/9ב = ❌ **עדיין** תלוי-מזל-איזה-פארק-נמצא-ראשון — לא תוקן, בעיה שונה (בחירת-פארק, לא גייט-תוצאה). 1-3 = הנדג'-בית תקין אבל כרטיס-המפה נכשל-בשקט (לא נבדק אם `500276ec` גם מכסה את זה — תרחיש-אחר, "אין פארק בכלל" לא "פארק לא-הידראולי"). 12 = אין מחרוזת "לא זיהינו פארק" בכלל בקוד. 15-16 = בית תקין, מפה נכשלת-בשקט (locationError נכתב, לא נקרא לפני מסך-אימון-פעיל). טבלה מלאה במקור: "2026-08-07__מצב-אמת-מאוחד" (דרייב).

---

## חלק ד' — המחולל עצמו (מנוע-ההצעות, §11)

**מקור:** "2026-08-07__ארכיטקטורת-מנוע-האימונים-וההצעות__מעודכן" (דרייב, מקור 28.7, עודכן 7.8 בסעיף 15 — **אומת פעמיים באופן עצמאי** ב-7.8 ע"י שתי חקירות-קוד נפרדות שהגיעו לאותה מסקנה).

### ⚠️⚠️ תיקון-מהותי 15.08.2026 (מאוחר יותר באותו יום) — שלב 5 כבר לא ריק, טעיתי בדיווח קודם היום

בדוח קודם היום דיווחתי לדוד "שלב 5 (מנוע-ההצעות) מאושר-ריק, לאף משטח" — **זה היה נכון ב-7.8, אבל התיישן**. תוך-כדי סקופינג ל-C3 (כרטיס-הליכה לפי יעד-צעדים) מצאתי **סדרת-עבודה שלמה ("rec-engine", ~30 commits, prefix `workout-engine/rec-engine`) שבנתה בפועל את שלבים 1+2+3+5 של §11**, ולא הייתה בשום מסמך-דרייב שקראתי (מאוחרת ל-7.8). לא הצלבתי את זה מול git לפני שדיווחתי — בדיוק אותו-סוג-כשל כמו פרשת-הפוש. מתקן עכשיו:

- **§11.1 (חוזה):** ✅ בנוי — `UserContext`/`Suggestion`/`ProtocolMethod`/`Generator` (`335df8b6`).
- **§11.2 (שיטות):** ✅ בנוי — pyramid/superset/tabata retrofit.
- **§11.3 (מחוללים):** ✅ **5/5 בנויים** (`generator-registry.ts`): `route` (הליכה/מסלול!), `route-stops`, `full-park-workout`, `full-strength`, `anchor-loop`.
- **§11.5/§8.1 (מנוע+דירוג):** ✅ בנוי — `suggestion-engine.ts`'s `runSuggestionEngine()` (context→eligible-generators→ranker, גנרי-לגמרי, לא map-specific), `rank-suggestions.ts` (כולל `stepDeficit` scoring אמיתי מ-`context.stepGoal`/`stepsRemaining`).
- **מחובר לבית? לא. מחובר למפה? חלקית-בלבד, לא כמו-שנשמע:** `buildMapUserContext`+`useSuggestionEngineStore` **כן** רצים חי ב-`DiscoverLayer.tsx` (מאחורי `MAP_REC_ENGINE_RANKING_V1=true`) — **אבל** `applyRankedSlotOrder` רק **מתייג** (badge `recommended:true`) אחד מ-3 slots-קיימים-מראש (`anchor-loop`/`full-park-workout`/`route-stops` בלבד — `GENERATOR_ID_TO_SLOT_ID`). **`'route'` (המחולל הרלוונטי ל-C3) לא ממופה לשום slot קיים — מייצר Suggestion תקין, אבל כרגע נזרק, גם במפה.** המנוע לא-עדיין יוצר כרטיס-חדש-מאפס משום Suggestion, בשום משטח — רק מתייג-מחדש בין-קיימים. חשוב-לדעת-בדיוק את הגבול הזה, לא לעגל-כלפי-מעלה.

**המשמעות ל-C3 (ולכל עבודה עתידית על "הבית"):** הבסיס בשל-בהרבה ממה שחשבתי הבוקר. ראה חלק ה' למטה.

### סדר-בנייה מומלץ (§11, לא-שונה, מאושר):
1. החוזה קודם (UserContext, Suggestion, ממשק-שיטה, ממשק-מחולל, תצוגה-כפולה) — ✅ **בנוי**
2. התאמת שיטות קיימות לחוזה — ✅ **בנוי**
3. התאמת מחוללים קיימים לחוזה — ✅ **בנוי (5/5)**
4. בניית שיטות חסרות (AMRAP/EMOM/אינטרוולים) — עדיין חסר
5. **בניית מנוע-ההצעות (PULL)** — ✅ **בנוי** (לא "ריק" — ראה תיקון למעלה); לא-מחובר לבית, מחובר-חלקית-בלבד למפה (badge-only)
6. בניית המחולל ההיברידי (AMRAP)
7. שכבת הרגעים (PUSH)
8. מנוע הריצה — **הכי-גדול, אחרון בכוונה**
9. שכבות חכמות (למידה)

### מה כבר קיים-לשימוש-חוזר (§15, לא ניחוש — מאומת קוד פעמיים):
- `WorkoutGenerationContext` **כבר** ה-UserContext המשותף בפועל — אותו import ממש בבית (`home-workout.service.ts`) ובמפה (`compose-hybrid-session.service.ts`), לא "דומה".
- `composeHybridSession` — הכי-קרוב-למנוע-קיים, כבר גנרי, כבר-משותף ל-2/3 נתיבי-מפה.
- `getWeeklyLoadSnapshot`→`{aerobicGapMin, strengthGapDays, neglectedDomains}` — כמעט-מילולית "יעד-שנותר", כבר מזין גם היברידי וגם BudgetDistributor בבית. **הבסיס-הקיים-הכי-חזק לחזון.**
- `dispatchStopContent`, `resolveStationContent`/`buildHydraulicExercise`, `assignVolume`/`TIER_TABLE`/`DIFFICULTY_VOLUME`, `resolveHybridUserLevels`, `deriveAerobicTargetKm` — כולן חתיכות-פאזל קיימות.

### 3 כפילויות-אמיתיות-לאיחוד (לא הבדל-מוצרי):
גייט (הערכה→הידראולי→חסימה) כתוב-ביד 3 פעמים סביב אותן פונקציות; `fetchRealParks`+fallback משוכפל מילולית 3 פעמים; אובייקט-ברירת-מחדל-פערים-שבועיים זהה-מילולית בשני נתיבים.

### 3 אי-עקביות דורשות-החלטה (לא לאחד אוטומטית):
דרגות-קושי לא-אחידות (full-park=3 גרסאות, route-stops=3 קומפוזיציות-חוזרות, מומלץ/budget-split=**אין קושי בכלל**); Block-B מחשב "יעד-יומי-שנותר" בנפרד מ-`remainingWeeklyBudget` הקיים (אותה מציאות עסקית, שני מקומות); מתמטיקת-קצב/MET אירובית משוכפלת **בכוונה** בין שני שירותים (מתועד בקוד כבידוד-מכוון).

### הפער-האמיתי-היחיד:
ממשק-אינטגרציה בית↔מפה — לא קיים כלום שקורא ממנוע-אחד-לשני היום.

**המלצה מהמסמך עצמו (חלק ו', לא ניחוש שלי):** לפני בנייה — שלב-מיפוי **read-only** (לא בנייה) שממפה בדיוק כמה מהלוגיקה בשלושת-נתיבי-המפה + Block-B היא כפילות-הניתנת-לאיחוד מול הבדל-מוצרי-אמיתי, ומה גודל-המשימה האמיתי. זה נותן תמונה לפני התחייבות לתוכנית-בנייה גדולה.

**תרחיש-משתמש (למה זה משנה):** דוגמה מפורשת מהמסמך: "סיים אימון כוח בפארק → דף בית → post_workout: משלים קצר או בטן, לפי מה שחסר וכמה זמן/צעדים נשארו." **זה לא קורה היום** — הבית והמפה הם שני מחוללים-עצמאיים, אין ביניהם שום דירוג/תיאום. משתמש שעשה חצי-אימון במפה חוזר לבית ומקבל המלצה שלא "יודעת" מה קרה.

---

## חלק ה' — feat/home-daily-goal-v1: להעלות ל-Stage-1 או לא

### מצב עדכני (15.08.2026, מאומת git טרי):

- **מרחק מ-main: 448 קומיטים מאחורי / 37 קדימה.** (היה 148/37 ב-7.8 — main התקדם ~300 קומיטים תוך 8 ימים. בדיקת merge-tree-סימולציה מ-7.8 מצאה קונפליקט-טקסטואלי-יחיד בלבד — **אבל זו בדיקה בת 8 ימים, חייבת-חזרה לפני שסומכים עליה**.)
- **4 דגלים, לא 2-3:** `HOME_DAILY_GOAL_V1`, `POST_WORKOUT_LANDING_V1`, `HOME_DAY_SELECT_SCOPE_V1`, `BLOCK_B_SMART_CLOSE_V1` — כולם `false`, אף משתמש חי לא נחשף.

| # | דגל | קומיטים | מה זה בפועל |
|---|---|---|---|
| 1 | `HOME_DAILY_GOAL_V1` | 7 | תשתית-מעקב-יעד-יומי: סף-השלמה ⅔-מהסטים (**משנה את המשמעות של "אימון הושלם"**), פס-כוח%, פס-אירובי-7-ימים, דה-דופ-דקות (client-side — כפילות אמיתית ב-server `ingestHealthSamples` עדיין פתוחה) |
| 2 | `BLOCK_B_SMART_CLOSE_V1` | 17 | ראה למטה — נפרד-במהות מ-level-equivalence (PR#29) |
| 3 | `POST_WORKOUT_LANDING_V1` | — | הלנדינג-אחרי-אימון (חלק מ-Block-B) |
| 4 | `HOME_DAY_SELECT_SCOPE_V1` | — | תיקון-באג "פס-הימים קפוא אחרי אימון" |
| — | **ללא דגל בכלל** | **8** | workout-budget/completion fixes — **משנים התנהגות-יצירה חיה לכל משתמש מיד-עם-מיזוג**, כולל revert-אחד+תיקון-נכון-שני. דורש החלטה נפרדת. |

**מה חי בפועל מ-Block-B:** רצועת-סיכום-עליונה (ring+זמן+קלוריות) ✅, כרטיס-התאוששות גנרי-קבוע (אותו וידאו תמיד, לא-תלוי-הקשר) ✅, קליטת-נתונים ✅. **מה מת:** `postWorkoutSuggestion.ts` (הבורר החכם) בנוי אבל `PostWorkoutSuggestionCard` — אפס-consumers, קוד-מת. "hero + 2 alternatives" — אין assembler. קומנט-בקוד מפורש: "wave-1 is THIN... full assembler later".

**בדיקות:** TSC נקי, vitest=baseline, **אפס טסטים חדשים על 37 קומיטים**. אין עדות ל-device-test שהקוד-עצמו דורש ("do NOT enable in prod until David has verified").

### ⚠️ מתח אסטרטגי אמיתי — לא רק "לדחוף או לא"

מסמך "מצב-אמת מאוחד" חלק ד' (7.8.2026, ניתוח-חיבור מפורש): **Block-B הוא ניסיון-ראשון-לא-גמור לאותו מנוע-פאזל-משותף** (חלק ד' למעלה) — רק שנבנה כתכונה-בודדת-לדף-הבית, לא כמנוע-משותף. **המלצה מפורשת במסמך:** "לא לגמור את Block-B כפרויקט-נפרד — לתכנן מנוע-החלטות אחד שמזין גם הצעה-אחרי-אימון בבית וגם כרטיסי-מפה." כלומר: דחיפת `BLOCK_B_SMART_CLOSE_V1`+`POST_WORKOUT_LANDING_V1` כמו-שהם מסתכנת בבניית גרסה-שנייה-מקבילה בדיוק למה שחלק ד' (המחולל) אמור לאחד.

**ההצעה שלי (לדיון, לא החלטה):** לפצל את 4 הדגלים לשתי קבוצות —
- **(א) `HOME_DAILY_GOAL_V1` + `HOME_DAY_SELECT_SCOPE_V1`** — תיקוני-UI/מעקב צרים, לא-שזורים בחזון-המנוע. כנראה סביר ל-rebase+device-test+דחיפה בנפרד.
- **(ב) `POST_WORKOUT_LANDING_V1` + `BLOCK_B_SMART_CLOSE_V1`** — לפי ההמלצה-האסטרטגית של המסמך עצמו, עדיף **לא** לגמור standalone — לקפל לתוך עבודת-המנוע (חלק ד') כשהיא תתחיל, במקום להשקיע עוד בגרסה-המקבילה-הישנה.
- **8 הקומיטים-הלא-מוגנים** — החלטה נפרדת-לגמרי, בלי קשר לשאר, כי הם כבר-משנים-התנהגות-חיה ברגע-המיזוג.

**תרחיש-משתמש (Stage 1 בלבד — HOME_DAILY_GOAL_V1):** משתמש רואה פס-התקדמות לעבר יעד-כוח/אירובי שבועי, וסף-חדש (⅔ מהסטים, לא 100%) שקובע מתי אימון "נחשב הושלם" בלוח-הבית שלו — משנה חוויה-בסיסית (מתי אני מרגיש שסיימתי) לכל מי שנחשף. **תרחיש-משתמש (Block-B כמו-שהוא):** משתמש מסיים אימון ומקבל המלצת-המשך שבפועל מוגבלת-מאוד (כרטיס-התאוששות גנרי-קבוע, לא באמת "חכם") — כי ה-assembler האמיתי לא קיים. זה בדיוק מה שהמנוע המשותף (חלק ד') אמור לפתור נכון, בפעם הבאה.

---

## חלק ו' — תוכנית 3.8.2026 (בריפי-מחקר + panel backlog)

**נמצא:** "workplanaerobicstrength1_5weeks.md" (דרייב, נוצר 3.8.2026 07:24). **סטטוס אמיתי: כבר בוצעה ברובה, לא ממתינה.**

### שכבה 1 — 3 בריפי-מחקר, מצב ביצוע:
- **בריף A** (מיפוי כל-מה-שלא-נדחף, כל worktree) — ✅ **בוצע**, זה בדיוק מה שהוביל לגילוי+גיבוי-החירום של `feat/home-daily-goal-v1` (7.8.2026, ראה חלק א'+ה').
- **בריף B** (מיפוי-עומק אירובי/מסלולים, טבלת חתיכות-פאזל) — ✅ **בוצע**, זה בדיוק מקור עץ-ההחלטות (5.8) + מסמכי-הסטטוס (6-7.8) + הממצאים בחלק ג'-ד' למעלה.
- **בריף C** (ולידציה עמוקה מנוע-הכוח — כולל **בדיוק** שאלת level-gating שביקשת לוודא) — ❓ **לא נמצא מסמך-פלט ייעודי.** ייתכן שלא רץ מעולם, או שהתוצר שלו לא-עדיין-אותר. **סעיף 2 שלו (level-gating) נענה עכשיו ישירות** ע"י האימות הטרי בחלק ג' למעלה — אבל סעיפים 1/3/4/5 (התאמת-זמן-לתוכנית, הגיון-משך-אימון, החלפת-סרטונים בית⇄חוץ, הצעת-המשך-בסיום) **נשארים לא-מאומתים**. מועמד זול להרצה-נפרדת בעתיד, לא דחוף — שום דבר לא מצביע היום שהם שבורים.

### שכבה 2 — panel backlog, 6 פריטים, **לא אומת מחדש היום** (סטטוס כפי-שתוכנן ב-3.8, לא נבדק מול קוד):
1. שאלון בטן חדש בפאנל (לא קיים) — בינוני
2. דיוק שאלון-הרשמה — קטן-בינוני
3. שאלון-השלמה + ניתוב-אוטומטי לרגליים-בלבד — בינוני
4. דיוק מסך-סיכום (כוח/אירובי/משולב, 3 גרסאות) — בינוני-גדול
5. מסך עליית-אחוזים-בכוח איטי — קטן, לבדוק ביצועים-מול-לוגיקה לפני בנייה-מחדש
6. בדיקות מלאות שאלון+אימוני-קטלבלס — קטן-בינוני

### הערכה-כנה של המסמך המקורי (3.8, ציטוט ישיר):
"ריאלי בשבוע וחצי: 3 בריפי-המחקר + שכבה 2 כמעט-כולה + 2-3 תיקונים-דחופים. **לא-ריאלי:** לבנות בפועל את כל חתיכות-הפאזל שטבלה B תעלה + לסיים גם את שלב-הריצה." **המלצה מהמסמך:** לסיים את הסבב עם 3 טבלאות + שכבה-2 סגורה + התיקונים-הדחופים, כיסוד ליציב לתכנון הבא.

**היכן אנחנו ביחס לזה, בפועל (15.08, 12 יום אחרי):** 2/3 בריפים בוצעו ביסודיות (חורגים-בהיקף ממה שתוכנן — כל עץ-ההחלטות+האימותים בחלק ג'-ד'). שכבה-2 (panel backlog) — **אין עדות שנגעו בה בכלל**. זה בדיוק הפער שהסבב-הבא (מה שדוד מבקש עכשיו) צריך לסגור: תעדוף אמיתי, לא ניחוש.

---

## חלק ז' — רצף-עבודה מוצע (הצעה לדיון, לא החלטה)

סדר-הבנייה שלמטה מכבד את סדר-הבקשה של דוד (אותיות → כרטיס-אירובי → מחולל → home-daily-goal-v1 → תוכנית-3.8), עם המלצת-עדיפות שלי בתוך/בין השלבים:

1. **A2 — לברר-למה-הוחזר (שאלה לדוד), ואז לתקן-מחדש/לגשת-אחרת.** עדיפות גבוהה — באג-שלמות-נתונים בפיצ'ר-דגל, כבר-מאובחן, תיקון כבר-קיים בהיסטוריה.
2. ~~בדיקה זולה: האם תרחישים 5-6/8/9א/9ב כבר-תוקנו~~ — **בוצע 15.08 (בדיקת-המשך אוטונומית).** 5-6/8 מאושרים-תוקנו בפועל (`500276ec`, חי, מאומת-diff+טסטים). 9א/9ב **עדיין לא** — בעיה שונה (בחירת-פארק בין-מועמדים, לא גייט-תוצאה), עדיין תלוי-מזל. אם רוצים לתקן את 9א/9ב — זו עבודה חדשה, לא "בדיקה זולה".
3. **החלטת-C4 (עכשיו, ממך)** — עלות-אפס לסגור, מנקה קוד-מת.
4. **שלב-המיפוי read-only שהמסמך-עצמו ממליץ עליו** (חלק ד', לפני-בנייה) — זול, נותן-תמונה-אמיתית של גודל-מנוע-המשותף לפני התחייבות.
5. **feat/home-daily-goal-v1: rebase טרי + החלטת-פיצול** (חלק ה') — אי-אפשר לפסוק "רלוונטי כמו-שהוא" על סמך snapshot בן-8-ימים; לפצל את ההחלטה כמוצע.
6. **C1-ב + C2** — משימות-scoping/בנייה קטנות, לא-דחופות, לתזמן-מתי-שנוח.
7. **סעיפי בריף-C הנותרים (1/3/4/5)** — בדיקת-ביטחון זולה למנוע-הכוח, לא דחוף.
8. **Panel backlog (6 פריטים, שכבה 2 מ-3.8)** — כפי-שדוד עצמו סידר, אחרון.
9. **בניית-המנוע עצמו (§11, שלבים 1-9)** — **רק** אחרי שלב 4 לעיל נותן תמונה אמיתית של ההיקף; זו התחייבות-רב-שבועית, לא-להתחיל-בניחוש.

---

## חלק ח' — C3: כרטיס-הליכה/אירובי ליום-מנוחה לפי יעד-צעדים (15.08.2026, בקשת דוד/מוני)

**התרחיש:** ביום בלי אימון-כוח מתוכנן, דף-הבית מציג כרטיס-הליכה/אירובי חדש מבוסס-כמה-צעדים-נשארו-ליעד — **לצד** הכרטיסים הקיימים (כוח/סרטון-התאוששות), לא במקומם.

**התגלית המרכזית:** יש כבר מחולל אמיתי, בנוי, רשום, עם `surfaces: ['map', 'home']` **מפורש בקוד** — `routeGenerator` (`route.generator.ts:25-28`) — שכבר:
- מחשב `targetDistance` מ-`deriveAerobicTargetKm` **כולל `stepsRemaining`/`stepGoal` אמיתיים** (`route.generator.ts:60-64`) — הבוסט-לפי-חוסר-צעדים כבר-מחובר, לא רק קיים-תיאורטית.
- קורא ל-`fetchRealParks()`+`generateDynamicRoutes()` (בדיוק אותה פונקציה כמו free-run).
- מחזיר `Suggestion` תקין (`type:'daily_workout'`, `surfaceEligibility:['map','home']`).
- "dark until something calls generate()" (קומנט-בקוד עצמו) — **אף אחד לא קורא לו מהבית היום.**

**מקור-הצעדים האמיתי (קיים, לא-לבנות):** `buildStepContext(daily: DailyActivity)` (`build-step-context.ts:17-26`) — `stepGoal=daily.stepsGoal` (ברירת-מחדל `DAILY_STEP_GOAL=8000`, נדרס ע"י `activityPriorityService.getDailyGoals` אם יש תוכנית פעילה), `stepsRemaining=max(0, stepGoal-stepsToday)`. פונקציה טהורה, קלט=`useActivityStore.getState().today`.

**3 הפערים האמיתיים (לא 4 כמו-שחשבתי-הבוקר — הרביעי כבר-סגור):**
1. **אין `buildHomeUserContext`.** קיים רק `buildMapUserContext` (`build-map-user-context.ts`) — למפה. צריך מקביל-לבית: `profile`+`buildStepContext(useActivityStore.getState().today)`+`surface:'home'`+`location` (אופציונלי-בהתחלה, ה-generator בודק `eligible: context.location !== null` ממילא). **תבנית-קיימת-להעתקה, לא-לתכנן-מאפס.**
2. **שום קריאה מהבית ל-`runSuggestionEngine`.** צריך אתר-קריאה חדש (סביר: `home/page.tsx` או `StatsOverview.tsx`, לצד הרינדור-הקיים של כרטיסי-הטריו) שבונה context ואז קורא ישירות ל-`runSuggestionEngine(context)` — **לא** דרך `useSuggestionEngineStore`/`applyRankedSlotOrder` (אלה map-specific, בונים על 3-slots-קיימים-מראש שלא-קיימים בבית). לבית: להשתמש ב-`Suggestion` שחוזר ישירות.
3. **רינדור:** ה-`Suggestion` (`title`, `structure.durationMin`, `goalTags`) לא="כרטיס". שתי אפשרויות: (א) לקרוא ל-`generateDynamicRoutes` ישירות מהבית (כמו שתוכנן לפני-הגילוי-של-`routeGenerator`) ולהזין ל-`RouteCardUnified.tsx` (הרכיב האמיתי-חי, מאומת: 4 אתרי-רינדור אמיתיים — `RouteCarousel`/`BottomJourneyContainer`/`HybridSlotCarousel`/עצמו — `RouteCard.tsx` המקביל dead); (ב) לקרוא ל-`routeGenerator.generate(context)` ישירות (עוקף את `runSuggestionEngine`'s eligible-filter המיותר-כשיש-generator-בודד) ולמפות את ה-`Suggestion` ל-`RouteCardUnified`'s props. **(ב) עקבי-יותר עם חזון-המנוע-המשותף** (למה לבנות מסלול-חדש-לחלוטין כשיש generator מוכן עם אותה בדיוק לוגיקה) — אבל דורש מיפוי `Suggestion→RouteCardUnified-props` שלא-קיים-עדיין (ה-Suggestion לא-שומר route-object מלא, `top`/`routes[0]` מ-`generateDynamicRoutes` נזרק אחרי חילוץ 4 שדות). **פתוח לדיון-קצר, לא-סתום.**

**מה כבר-לא-פער (בניגוד למה שדווח לפני-הבדיקה-הזו):**
- ~~"אין פונקציה עצמאית צעדים→מרחק"~~ — יש (`deriveAerobicTargetKm`+`stepGapBoost`), וכבר-מחובר בפועל בתוך `routeGenerator`.
- ~~"route.generator.ts הצהרתי-בלבד"~~ — לא נכון יותר; זה generator אמיתי, רשום, נבדק (יש טסטים ל-suggestion-engine/rank-suggestions).

**`useGoalCelebration` — לא-רלוונטי-ישירות לבניית-הכרטיס עצמו.** זו תשתית-חגיגה גנרית (קונפטי+צליל, session-dedup) — לא ספציפית-לצעדים. רלוונטית **רק** אם רוצים "חגיגה כשמשלימים-יעד-צעדים דרך המסלול המוצע" — תוספת נחמדה, לא חלק מ-v1.

**`feat/home-daily-goal-v1` — לא שווה למשוך ממנו, לבנות ישירות על main.** בדיקת diff ישירה: הענף **קודם-לגמרי** לכל תשתית ה-rec-engine (`route.generator.ts`, `generator.types.ts`, `user-context.types.ts`, `useSuggestionEngineStore` — כולם מופיעים כ"נמחקים" ב-diff main→הענף, כלומר פשוט לא-קיימים שם). למשוך משם = לבנות-על-בסיס-ישן-יותר ואז-לרבייס נגד כל-זה בכל-מקרה. עדיף-בבירור להתחיל-נקי מ-main.

### היקף משוער

**לא "תיקון-קטן-ומוכל כמו C1-ב"** — C1-ב היה תיקון-קריאה-בקובץ-בודד. זה בנייה-חדשה, אבל **קטנה-יחסית** בזכות כמה-שכבר-קיים:
- שלב 1 (`buildHomeUserContext`) — קטן, תבנית-מוכנה-להעתקה.
- שלב 2 (אתר-קריאה בבית) — קטן-בינוני, hook/effect חדש + state לתוצאה.
- שלב 3 (רינדור) — תלוי-בהחלטה (א)/(ב) למעלה; (ב) קצת-יותר-עבודה (מיפוי חדש) אבל נכון-יותר ארכיטקטונית.
- **מעבר לזה:** תנאי-הצגה (רק-ביום-מנוחה? מיקום בדף?), UX-בקשת-מיקום (יש תקדים-ישיר, `WorkoutLocationSuggestions.tsx:81`), ודגל-חדש (`HOME_STEP_DEFICIT_CARD_ENABLED` — כבר-שם-מוצע קודם).

**סדר-גודל: יחידת-עבודה בינונית, לא-ענקית** — 3-4 קבצים חדשים/נגועים (context-builder חדש, home-page hook, החלטת-רינדור, flag), לא רה-פקטור-רחב.

### ✅ 15.08.2026 — אושר ונבנה (3 commits, מקומיים, לא-נדחפו)

דוד אישר אופציה **א'** (`generateDynamicRoutes` ישיר → `RouteCardUnified`, לא בונים מיפוי-ל-Suggestion חדש). נבנה:
1. `d54a8cc3` — `buildHomeUserContext` (מקביל-מדויק ל-`buildMapUserContext`) + `HOME_STEP_DEFICIT_CARD_ENABLED=false`.
2. `00add5fb` — `useStepDeficitRoute` hook: GPS רך → context → `deriveAerobicTargetKm` → `generateDynamicRoutes`.
3. `b97834b9` — חיווט-רינדור ב-`StatsOverview.tsx`, בתנאי `flag && trioResult.isRestDay && route` — לצד ה-hero-card, לא במקומו.

**נסקר פעם ראשונה** ע"י code-reviewer agent — PASS, אפס ממצאים. **דוחף** (16.08.2026): 4 commits (כולל C1-ב) עלו ל-`origin/main` — `c99701f9`-מקדים (FF נקי, אין קונפליקטים, כלל-לא-שלי).

### ✅ 16.08.2026 — עדכון-המשך: נמצא מנגנון-קיים למנגנון-ההעברה, לא צריך לבנות חדש

תוך-כדי חקירת-ההחלטה השנייה (לחיצה→מסך-מפה-עם-המסלול-הזה-מוצג) התגלה: **יש כבר deep-link אמיתי, חי, מאומת-על-מכשיר** — `/map?openRun=walking&targetSteps=N` (`DiscoverLayer.tsx:390-407`, מאחורי `IS_STEP_GOAL_ROUTE_PREVIEW_ENABLED=true`, **הופעל-מחדש 12.8** אחרי שהאבחון הקודם (`step-goal-route-preview-regression.md`) התברר כבאג-סדר-אתחול-native, לא-קשור לקוד-המפה/מסלולים) — בדיוק ההתנהגות שדוד ביקש: מדלג על שלב-הקונפיג, קופץ ישר ל-`RouteCarousel` בגודל-המטרה. **אין gate לפי-uid בקוד-הצריכה עצמו** (ה"David-בלבד" הוא רק בצד-שליחת-הפוש) — כלומר זה יעבוד לכל משתמש שינחת על ה-URL הזה, לא רק דוד.

**מסקנה: לא בונים מנגנון-חדש — ה-CTA משתמש באותו deep-link בדיוק**, מופעל מלחיצה-בבית במקום מפוש. תוקן (לא-נדחף עדיין):
- `useStepDeficitRoute.ts`: `stepsToTargetKm` (route-request.utils.ts — הנוסחה שה-deep-link עצמו משתמש-בה) **במקום** `deriveAerobicTargetKm` — כדי שהתצוגה-המקדימה בבית תואמת בדיוק את מה ש-`/map` יבנה בפועל בלחיצה, לא נוסחה-שנייה-שיכולה-לסטות. גם `surface:'road'`+`shortRouteMode:IS_STEP_GOAL_SHORT_ROUTE_ENABLED` נוספו לאותה סיבה.
- `StatsOverview.tsx`: ה-CTA משתנה מ-`router.push('/map')` ל-`router.push('/map?openRun=walking&targetSteps=' + stepsRemaining)`.

**החלטת-עיצוב-עדיין-פתוחה, לא-guess:** ה-CTA `<Footprints/>` + "הליכה עד היעד" — **אושר ע"י דוד 16.8** כמו-שהוא לגרסה-הנוכחית; follow-up-מתועד (לא-דחוף) לבנות מערכת-טקסט-מתחלף/מודולרי בעתיד.

**🔒 שימור-חזון (דוד, 15.08.2026, לתעד כדי שלא-ילך-לאיבוד):** האישור לאופציה א' (16.8: הרחבה זו כוללת) הוא **קיצור-דרך פרגמטי לכרטיס הזה בלבד**, לא ויתור על חזון-מנוע-משותף. אם בעתיד המחולל יחזיר הצעה-משולבת (למשל 30%-כוח+70%-אירובי, לשרת גם-יעד-כוח-וגם-יעד-צעדים במסלול-אחד) — זו **חייבת** להיות אותה-הצעה-בדיוק בין אם מוצגת בבית או במפה, לא שני-מנגנונים-שיכולים-להתרחק. `build-home-user-context.ts`'s doc-comment מתעד את זה גם בקוד, אבל **הבנייה העתידית של הצעות-משולבות צריכה לחזור-לכאן** ולוודא ש-`buildHomeUserContext`/`buildMapUserContext` נשארים מסונכרנים, ושכל מנגנון-חדש מוחל **פעם-אחת, לשני המשטחים**, לא נבנה נפרד-לכל-משטח.

**נסקר-שוב** ע"י code-reviewer agent על התיקון הזה (תוצאה בדוח לדוד/מוני). **לא נדחף** — ממתין לאישור נפרד, כי זה שינוי חדש שנעשה אחרי ה-push הקודם.

## מקורות (כל המסמכים שנקראו בסנכרון הזה)

| מסמך | תאריך | Drive file ID |
|---|---|---|
| מסמך-אב עץ-החלטות כרטיסי-אימון | 5.8.2026 | `1ma7RKIU2MOVmRAcRZ5pYnvJso1kYsa0TN6iht9RUUHs` |
| decision-tree-current-vs-desired | 6-7.8.2026 | `1qx-reJ7Xm-2F4dLgwV3mUkON0lCF3Oqy9s2iFXd-Z7w` |
| ארכיטקטורת מנוע-האימונים-וההצעות (מעודכן, כולל §15) | 28.7+7.8.2026 | `1ptaO80rUH0eGyFkR4naMw-d4xoBbFGkzzAiCA6v5YHs` |
| suggestion-engine-brainstorm | 6.8.2026 | `1tq-uh7MG4YXaD7wlsAOlD_PEEernnSSvKWFN1IOjH-Y` |
| מצב-אמת מאוחד (כולל חלקים C-F במלואם) | 7.8.2026 | `12lcjG-Y5ynDvgkhRpAJMaypDA7KUcDVChrQNVDZh1wA` |
| תוכנית-עבודה 3.8 (workplanaerobicstrength1_5weeks) | 3.8.2026 | `111Jk_ag6sBDNcMMFt1ercNDl9MKaZEr7XirRjhzTX20` |
| `.claude/plans/happy-jumping-flask.md` (מקומי) | עדכון 13.8.2026 | — |
| אימות git ישיר (A2, home-daily-goal-v1, גייט-אימון-משולב) | 15.8.2026 | — |
